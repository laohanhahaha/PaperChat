# PaperChat 开发计划

> 更新日期：2026-04-19

---

## 总体路线

```
Phase 1 — Chat 统一入口 + 关键优化（1-2 周） ✅ 已全部完成
  ├── 【功能】Agent 意图识别上线（已有后端，前端接入） ✅
  ├── 【功能】写作/分析/知识库功能收拢到 Chat ✅
  ├── 【功能】阅读辅助面板集成 ✅
  ├── 【优化】ws.py 拆分 · Alembic 迁移 · PDF 懒加载 · ReaderPage 并行化 · BM25 缓存 ✅
  ├── 【优化】提示词分离 · SessionService/MessageService 提取 · WebSocket 认证 ✅
  └── 【优化】上下文压缩机制（对话 Token 预算管理 + 自动摘要压缩） ✅

Phase 2 — 智能化升级 + 架构解耦（3-4 周） ✅ 已全部完成
  ├── 【功能】多 Agent 研究助手（核心差异化功能） ✅
  ├── 【功能】简报生成 Agent（Briefing Mode）— 按频率自动推送学术简报 ❌ 待排期（依赖 Phase 4 搜索集群）
  ├── 【功能】知识图谱自动构建 ✅
  ├── 【功能】Chat 意图覆盖完善 ✅
  ├── 【优化】Markdown memo · 打字机效果 · 跨文档并行 · 消息虚拟化 · ChatPage 拆分 ✅
  └── 【优化】RAG/LLM 解耦 · Repository 层 · ToolContext 统一 · Store 拆分 ✅

Phase 3 — 体验打磨 + 性能调优（2-3 周）
  ├── 【功能】推荐系统增强
  ├── 【功能】批量分析功能
  ├── 【功能】用户控制与成本感知面板（模型选择器 · 费用确认弹窗 · 预算仪表盘 · 实时模型指示器）
  ├── 【功能】内嵌 WebView 组件（内部打开链接 · 简报展示）
  ├── 【优化】RAG 索引异步重建 · 全文缓存 · 前端请求缓存 · 消息分页
  ├── 【优化】依赖注入 · 分析缓存拆表 · 全局错误处理 · React.memo · WS 压缩
  ├── 【优化】启动性能优化（服务懒加载 · 并行初始化 · 启动 Profiling）
  └── 【优化】上下文压缩增强（自动摘要 · Token 预算管理 · 消息剪枝）

Phase 3.5 — 工程规范化基础设施（与 Phase 3 并行，1-2 周）
  ├── 【规范】提示词版本管理 · 结构化错误码体系
  ├── 【规范】工具权限分级控制 · Prettier + Pre-commit
  ├── 【规范】TypeScript strict 迁移 · 核心路径测试覆盖
  ├── 【规范】隐私与安全规范（数据最小化声明 · API Key 安全审计 · 匿名使用模式）
  ├── 【规范】多级配置体系（默认 → 用户 → 项目 → 服务端策略覆盖）
  └── 【基建】学术 MCP Server 工具库基础设施（MCP Manager + 协议适配层）

Phase 4 — 自主配置 Agent + 外部服务集成（5-7 月）★ 赛题功能开发
  ├── 【功能】ConfigAgent — 自主配置外部服务连接（适配 MCP 协议）+ 配置预览与安全确认
  ├── 【功能】学术 MCP Server 工具库上线（arXiv / S2 / CrossRef / DBLP / Zotero / 报告生成）
  ├── 【功能】多源搜索服务集群（学术源 + Tavily / Bing / 百度 / Brave / wigolo + SearchDispatcher 智能调度）
  ├── 【功能】引用管理集成（Zotero / Mendeley API）— 由 MCP Server 驱动
  ├── 【功能】结构化报告生成 Server（大纲/综述/对比分析自动生成）
  ├── 【功能】API Key 安全管理 + 服务健康检测
  └── 【功能】用户只需提供 API Key，Agent 自动完成配置/调试/调用（ConfigAgent + MCP Manager 协同）

Phase 5 — 多模态深度理解（7-9 月）
  ├── 【功能】图表解析（折线图/柱状图/散点图）
  ├── 【功能】公式识别（LaTeX 提取与语义理解）
  ├── 【功能】表格结构化提取
  ├── 【功能】多模态搜索（图表解析 → 提取关键实体 → 搜索集群 → 返回相关论文）
  └── 【功能】领域微调模型 + 评估体系

Phase 6 — AMD 锐龙 AI MAX+ 端侧适配 + 本地推理（9-10 月）★ 赛题统一适配
  ├── 【适配】AMD 锐龙 AI MAX+ 平台部署与优化
  ├── 【适配】ONNX DirectML / ROCm 推理后端集成
  ├── 【功能】智能路由与成本控制引擎（用户偏好 · 月度预算 · 单次确认 · 免费额度感知）
  ├── 【功能】隐私论文端侧强制路由 + 离线模式
  ├── 【功能】RAG 增强（Reranker + HyDE + 混合检索）
  ├── 【功能】离线优先与主动预缓存（离线队列 · 预缓存订阅 · 离线搜索）
  └── 【展示】端侧多 Agent 并发推理 + ConfigAgent 离线协同 + 性能基准测试

Phase 7 — 团队协作平台（11-12 月）
  ├── 【功能】组织与项目管理
  ├── 【功能】共享知识库 + 协同标注
  ├── 【功能】任务分配 + 研究看板
  └── 【功能】权限管理（管理员/成员/访客）

Phase 8 — 开放生态（次年 Q1）
  ├── 【功能】插件系统
  ├── 【功能】开放 API
  ├── 【功能】模板市场 + Agent 市场
  └── 【功能】Tauri 桌面端

Phase 9 — 基础设施建设（与功能开发并行，持续进行）
  │  注：Phase 3.5 已完成的基础规范（Prettier、TS strict、测试、错误码、权限分级、隐私安全、多级配置）
  │      不再重复列出，本阶段聚焦于运维、安全、性能等长期基建。
  ├── 结构化日志 · API 版本化
  ├── CI/CD · 请求限流 · 错误边界 · 国际化
  ├── 连接池调优 · 优雅关停 · 数据备份 · Feature Flag
  ├── 事件驱动架构 · 安全加固 · 数据库索引优化
  ├── 启动 Profiling · API 调用成本追踪 · UI 渲染性能监控
  └── 消除硬编码 user_id
```

### 赛题适配说明

> 第二十一届研电赛 AMD 企业命题方向一：**基于 AMD 锐龙 AI MAX+ 平台的端侧 AI 智能体与垂直行业创新应用**
>
> PaperChat 定位为"学术研究垂直领域"的端侧 AI 智能体，Phase 4-6 是参赛核心内容：
> - **Phase 4** 先完成赛题相关的功能开发（ConfigAgent + 外部服务集成）
> - **Phase 5** 多模态深度理解（图表/公式/表格 + 领域微调）
> - **Phase 6** 统一适配到 AMD 锐龙 AI MAX+ 平台（端侧推理 + 性能优化）
> - 报名截止：2026-06-20，初赛预计 9 月，决赛预计 10 月

### 产品演进路线

```
当前定位：个人论文阅读助手
    ↓ Phase 1-3
进阶定位：个人研究工作台（Agent + 知识图谱 + 写作）
    ↓ Phase 4 ★ 赛题核心
端侧定位：端侧 AI 智能体研究平台（AMD 锐龙 AI MAX+ 本地推理 + 自主配置 Agent）
    ↓ Phase 5
深度定位：多模态深度理解研究平台（图表/公式/表格 + 领域微调）
    ↓ Phase 6 ★ 赛题统一适配
端侧定位：AMD 锐龙 AI MAX+ 端侧推理 + 智能路由与成本控制 + 离线优先与预缓存
    ↓ Phase 7
团队定位：团队协作研究平台
    ↓ Phase 8
生态定位：学术研究基础设施（开放 API + 插件市场 + 社区）
```

### 技术架构演进

```
当前架构:
┌─────────────┐     ┌─────────────┐
│  React SPA  │ ←→  │  FastAPI    │ ←→ DeepSeek API
│  (Vite)     │     │  + ChromaDB │
└─────────────┘     └─────────────┘

赛题架构（AMD 锐龙 AI MAX+ 端侧智能体）:
┌─────────────┐     ┌──────────────────────────────────────────┐
│  React SPA  │     │  FastAPI                                 │
│  (Vite)     │ ←→  │  + ChromaDB                              │
│             │     │  + Agent Orchestrator                    │
│             │     │  + ConfigAgent（自主配置外部服务）         │
│             │     │  + Model Router（云端/端侧自动路由）      │
│             │     │  + MCP Manager（学术 MCP Server 统一调度） │
│             │     │                                          │
│             │     │  ┌────────────────────────────────────┐  │
│             │     │  │  AMD 锐龙 AI MAX+ 端侧推理引擎     │  │
│             │     │  │  ├─ ONNX DirectML (NPU+iGPU)      │  │
│             │     │  │  ├─ ROCm / llama.cpp (iGPU 96GB VRAM)│  │
│             │     │  │  ├─ 本地 RAG (ChromaDB)            │  │
│             │     │  │  └─ 多 Agent 并发调度              │  │
│             │     │  └────────────────────────────────────┘  │
│             │     │                                          │
│             │     │  ┌────────────────────────────────────┐  │
│             │     │  │  学术 MCP Server 集群（MCP Manager） │  │
│             │     │  │  ├─ arxiv-server（论文搜索与PDF下载）│  │
│             │     │  │  ├─ semantic-scholar-server（语义搜索）│  │
│             │     │  │  │  └─ 引用网络与影响力分析          │  │
│             │     │  │  ├─ crossref-server（DOI与元数据）  │  │
│             │     │  │  ├─ dblp-server（CS领域文献检索）   │  │
│             │     │  │  ├─ zotero-server（文献库双向同步）  │  │
│             │     │  │  ├─ report-generator-server（报告生成）│  │
│             │     │  │  │  └─ 大纲/综述/对比分析自动生成    │  │
│             │     │  │  └─ SearchDispatcher（多源智能调度） │  │
│             │     │  │     ├─ Tavily / Bing / 百度 / Brave │  │
│             │     │  │     └─ wigolo（本地免费搜索）        │  │
│             │     │  └────────────────────────────────────┘  │
│             │     │                                          │
│             │     │  外部服务（ConfigAgent + MCP Manager 协同）│
│             │     │  ├─ arXiv API                            │
│             │     │  ├─ Semantic Scholar API                 │
│             │  │  ├─ CrossRef API                         │
│             │     │  ├─ Zotero / Mendeley API                │
│             │     │  ├─ Tavily / Bing / 百度 / Brave API     │
│             │     │  └─ DeepSeek API（云端备用）             │
└─────────────┘     └──────────────────────────────────────────┘

远期架构（微服务 + 生态）:
┌──────────┐     ┌──────────────────────────────────────┐
│ 多端前端  │     │  API Gateway                         │
│ Web/Tauri│ ←→  │  ├─ Auth Service                     │
│ Mobile   │     │  ├─ Paper Service                    │
│          │     │  ├─ Chat Service (RAG + Agent)       │
│          │     │  ├─ Knowledge Graph Service (Neo4j)  │
│          │     │  ├─ Config Service (自主配置 Agent)   │
│          │     │  ├─ Model Service (云端/端侧路由)     │
│          │     │  ├─ Collaboration Service            │
│          │     │  └─ Plugin Service                   │
│          │     │                                      │
│          │     │  Message Queue (Redis/RabbitMQ)      │
│          │     │  Object Storage (MinIO)              │
└──────────┘     └──────────────────────────────────────┘
```

---

## 核心设计决策：Chat 作为统一入口

> 当前问题：功能散落在 6 个独立页面（Chat / Writing / Knowledge / Papers / Reader / Settings），用户需要在不同页面间跳转才能完成一个连贯的研究任务。
>
> 改进方向：Chat 成为 80% 交互的统一入口，Agent 意图识别自动路由到对应功能，用户只需"说一句话"。

### 当前页面 → Chat 统一入口后的变化

| 页面 | 当前定位 | Chat 统一入口后 | 变化 |
|------|----------|-----------------|------|
| ChatPage | 问答（RAG + 跨文档） | **统一入口**，承载 80% 交互 | ⬆️ 大幅扩展 |
| WritingPage | 大纲 / 对比 / 综述 | **移除独立页面**，功能由 Chat + Agent 覆盖 | 🗑️ 页面移除 |
| KnowledgePage | 知识卡片管理 | 保留为"知识库浏览"，创建/搜索由 Chat 触发 | ⬇️ 简化 |
| PaperList | 论文库管理 | 保留，简化为"论文管理"（上传/删除/批量） | ⬇️ 简化 |
| ReaderPage | PDF 阅读 | 保留，沉浸式阅读 + 侧边 Chat 面板 | 不变 |
| SettingsPage | 设置 | 保留 | 不变 |

### Chat 意图路由架构

```
用户在 Chat 中输入
  │
  ▼
Agent 意图识别
  │
  ├── 问答类 ──────────→ RAG 检索 + LLM 生成
  │   ├── "这篇论文讲了什么"         → simple_qa
  │   ├── "解释一下 attention"       → explain_term
  │   ├── "把这段翻译成中文"         → translate
  │   └── "总结第三章"               → summarize
  │
  ├── 分析类 ──────────→ 调用分析工具
  │   ├── "概述论文的章节结构"       → chapter_overview
  │   ├── "做深度分析"               → deep_analysis
  │   ├── "提取核心知识点"           → key_points
  │   └── "评估论文质量"             → quality_assessment
  │
  ├── 写作类 ──────────→ 写作工具 + 流式输出（原 WritingPage 功能）
  │   ├── "帮我生成一个大纲"         → generate_outline
  │   ├── "对比这两篇论文的方法"     → compare_content
  │   └── "帮我写一篇文献综述"       → literature_review
  │
  ├── 跨文档类 ────────→ 多论文 RAG（无需手动切换模式）
  │   ├── "对比论文 A 和论文 B"      → cross_doc（自动选论文）
  │   └── "这 3 篇论文有什么共同点"  → cross_doc（自动选论文）
  │
  ├── 知识库操作 ──────→ Knowledge API
  │   ├── "保存这个知识点"           → save_card
  │   └── "我之前关于 attention 的笔记" → search_cards
  │
  ├── 论文管理 ────────→ Paper API
  │   ├── "我最近读了哪些论文"       → recent_papers
  │   └── "帮我找关于 GAN 的论文"    → search_papers
  │
  ├── 外部服务 ────────→ ConfigAgent（Phase 5 新增）
  │   ├── "帮我连接 arXiv"           → config_service（用户提供 API Key，Agent 自动配置）
  │   ├── "从 arXiv 下载这篇论文"    → fetch_paper（Agent 调用已配置的外部服务）
  │   ├── "同步我的 Zotero 文献库"   → sync_library（Agent 调用 Zotero API）
  │   └── "哪些服务已连接"           → list_services
  │
  └── 研究助手 ────────→ 多 Agent 协作（Phase 2）
      └── "我在做超分，怎样提升性能"  → research_assistant
```

### 新增意图与工具

当前 Agent 已有 9 个工具，统一入口后需要新增以下意图和工具：

| 新增意图 | 触发示例 | 新增工具 | 说明 |
|----------|----------|----------|------|
| `save_card` | "保存这个知识点" | `SaveCardTool` | 调用 Knowledge API 创建卡片 |
| `search_cards` | "我之前关于 X 的笔记" | `SearchCardsTool` | 搜索知识卡片 |
| `recent_papers` | "我最近读了哪些论文" | `RecentPapersTool` | 查询论文列表 |
| `search_papers` | "帮我找关于 X 的论文" | `SearchPapersTool` | 搜索/推荐论文 |
| `literature_review` | "帮我写文献综述" | `LiteratureReviewTool` | 复用 RAG + LLM 长文生成 |
| `config_service` | "帮我连接 arXiv" | `ConfigServiceTool` | ConfigAgent 自动配置外部服务 |
| `fetch_paper` | "从 arXiv 下载这篇论文" | `FetchPaperTool` | 调用已配置的外部论文源下载 |
| `sync_library` | "同步我的 Zotero 文献库" | `SyncLibraryTool` | 调用 Zotero/Mendeley API 同步 |
| `list_services` | "哪些服务已连接" | `ListServicesTool` | 查询已配置的外部服务状态 |

**已有的意图无需改动**：`explain_term`、`translate`、`summarize`、`generate_outline`、`compare_content`、`chapter_overview`、`deep_analysis`、`key_points`、`quality_assessment`、`cross_doc` 均已在 `agent_service.py` 和 `unified_chat` 中实现。

### 跨文档模式简化

**当前**：用户需要手动切换"跨文档模式" → 选择多篇论文 → 输入问题。

**改进后**：Agent 自动判断是否涉及多篇论文。

```
用户: "对比论文 A 和论文 B 的方法"

Agent 意图识别:
  intent: "cross_doc"
  paper_ids: [自动从上下文/选中论文推断]

  → 如果用户已选中多篇论文，直接使用
  → 如果用户提到论文标题，自动搜索匹配
  → 如果无法确定，追问"你想对比哪几篇论文？"
```

---

## 一、Phase 1 — Chat 统一入口

> 目标：把 Chat 升级为统一入口，收拢 WritingPage 和散落的分析功能。

### 1.1 Agent 意图识别上线

**现状**：后端 `agent_service.py` 已实现 9 个工具 + 意图识别 + 任务规划，`ws.py` 的 `unified_chat` 已实现关键词路由。但前端 ChatPage 仍使用 `rag_chat` 消息类型，意图识别完全未启用。

**需要做的事**：

| 事项 | 说明 |
|------|------|
| ChatPage 接入 `unified_chat` 消息类型 | 替换当前的 `rag_chat`，让后端自动路由 |
| 处理意图识别反馈 | 展示 `intent_detected` 消息（如"📋 章节概述"标签） |
| 处理 Agent 流式消息 | `agent_intent` → `agent_plan` → `agent_step` → `agent_result` |
| 集成 AgentProgress 组件 | 展示 Agent 思考过程和工具调用（可折叠） |
| 新增意图对应的前端渲染 | 写作类结果用编辑器展示，知识卡片用卡片样式展示 |

**已有工具**（无需新开发）：

| 工具 | 功能 | 意图 |
|------|------|------|
| `search_text` | 论文文本检索 | search |
| `summarize` | 文本摘要 | summary |
| `translate` | 文本翻译 | translate |
| `explain_term` | 术语解释 | explain |
| `extract_key_points` | 要点提取 | key_points |
| `compare_content` | 内容对比 | comparison |
| `generate_outline` | 大纲生成 | outline |
| `assess_quality` | 质量评估 | quality_assessment |
| `get_paper_info` | 论文元数据 | — |

---

### 1.2 写作功能收拢到 Chat

**现状**：WritingPage 有三个独立 Tab（大纲生成 / 对比分析 / 综述撰写），用户需要专门切到该页面。

**收拢方案**：

| 原 WritingPage 功能 | Chat 中触发方式 | 实现方式 |
|---------------------|-----------------|----------|
| 大纲生成 | "帮我生成一个关于 X 的大纲" | `generate_outline` 工具，已有 |
| 对比分析 | "对比论文 A 和论文 B" | `compare_content` 工具，已有 |
| 综述撰写 | "帮我写一篇关于 X 的文献综述" | 新增 `literature_review` 工具 |
| 引用格式化 | "把这段引用格式化为 APA" | 新增 `cite_paper` 工具函数 |
| 文本润色 | "帮我润色这段文字" | 新增 `polish_text` 工具函数 |

**WritingPage 处理**：移除独立页面和导航入口。写作类结果在 Chat 中以**富文本卡片**形式展示，支持复制和编辑。

**待办事项**：

- [ ] 新增 `literature_review` 工具（RAG 检索 → LLM 长文生成）
- [ ] 新增 `cite_paper` / `polish_text` 工具函数
- [ ] Chat 中写作类结果的富文本渲染
- [ ] 移除 WritingPage 路由和导航入口
- [ ] 更新 `INTENT_KEYWORDS` 添加写作类关键词

---

### 1.3 知识库操作收拢到 Chat

**现状**：KnowledgePage 是独立页面，用户需要手动创建和管理知识卡片。

**收拢方案**：

| 操作 | Chat 中触发方式 | 实现方式 |
|------|-----------------|----------|
| 保存知识卡片 | "保存这个知识点" / "记下来" | 新增 `save_card` 工具 |
| 搜索知识卡片 | "我之前关于 X 的笔记" | 新增 `search_cards` 工具 |
| 浏览知识库 | 保留 KnowledgePage 入口 | 页面简化为纯浏览 |

**KnowledgePage 处理**：保留为"知识库浏览"页面，但移除创建/编辑入口（由 Chat 触发）。

**待办事项**：

- [ ] 新增 `SaveCardTool`（调用 Knowledge API）
- [ ] 新增 `SearchCardsTool`（搜索知识卡片）
- [ ] Chat 中知识卡片结果的卡片样式渲染
- [ ] KnowledgePage 简化为纯浏览视图

---

### 1.4 论文查询收拢到 Chat

**现状**：论文管理在 PaperList 独立页面，无法通过对话查询。

**收拢方案**：

| 操作 | Chat 中触发方式 | 实现方式 |
|------|-----------------|----------|
| 查询最近论文 | "我最近读了哪些论文" | 新增 `recent_papers` 工具 |
| 搜索论文 | "帮我找关于 X 的论文" | 新增 `search_papers` 工具 |
| 上传/删除论文 | 保留 PaperList 入口 | 需要文件选择器，不适合 Chat |

**PaperList 处理**：保留为"论文管理"页面，但简化为上传/删除/批量操作。

**待办事项**：

- [ ] 新增 `RecentPapersTool`
- [ ] 新增 `SearchPapersTool`
- [ ] Chat 中论文列表结果的列表样式渲染

---

### 1.5 阅读辅助面板集成

**现状**：`ReadingAssist` 组件存在但未集成到 PDFReader。

**需要做的事**：

| 事项 | 说明 |
|------|------|
| PDFReader 添加文本选择事件监听 | 选中文本后触发辅助面板 |
| 辅助面板定位 | 跟随选区位置弹出 |
| 复用 Agent 工具 | 术语解释、摘要、翻译等直接调用已有工具 |

**不需要新 Agent**：阅读辅助本质是"选中文本 → 调用对应工具"，是 Chat 统一入口在阅读场景下的一个快捷入口。

---

### 1.6 上下文压缩机制

> 长对话场景下消息列表无限增长，每次 LLM 调用都发送全部历史消息，既不经济也面临上下文窗口溢出风险。

**现状**：无任何压缩策略，历史消息完整保留并按顺序全部发送。

**方案**：三层上下文压缩策略（借鉴 Claude Code 的 compact 机制）

```
第 1 层 — 消息剪枝（零成本）
  ├── 删除连续重复的系统提示词
  ├── 合并相邻的 tool_use/tool_result 对为摘要
  └── 超过 N 轮后，丢弃最早的 turn（保留关键上下文）

第 2 层 — 摘要压缩（1 次 LLM 调用）
  ├── 触发条件：总消息数 > 20 轮 或 estimated_tokens > 窗口 60%
  ├── 将最早 50% 的消息压缩为 1 条摘要消息
  ├── 保留最后 2 轮完整消息（保证当前对话的上下文连贯）
  └── 摘要格式: [Compressed Summary] 用户问了 X，助手回答了 Y...

第 3 层 — 主动压缩（后台异步）
  ├── 在 LLM 空闲时，后台将历史消息预压缩
  ├── 压缩结果缓存，下次触发时直接替换
  └── 避免 LLM 响应时的压缩延迟
```

**技术实现**：

```python
class ContextCompressor:
    """上下文压缩器"""

    def __init__(self, llm_service):
        self.llm_service = llm_service
        self.max_turns = 20          # 触发压缩的轮次阈值
        self.max_token_ratio = 0.6   # 触发压缩的 token 阈值（窗口占比）
        self._cache = {}             # 后台预压缩缓存

    async def should_compress(self, messages: List, window_size: int) -> bool:
        """判断是否需要压缩"""
        token_count = estimate_tokens(messages)
        return (len(messages) > self.max_turns or
                token_count > window_size * self.max_token_ratio)

    async def compress(self, messages: List) -> List:
        """执行压缩"""
        keep_ratio = 0.5  # 保留最近 50%
        split_idx = int(len(messages) * keep_ratio)

        to_compress = messages[:split_idx]
        to_keep = messages[split_idx:]

        summary = await self._summarize(to_compress)
        return [summary] + to_keep

    async def background_precompress(self, session_id: str, messages: List):
        """后台预压缩（异步非阻塞）"""
        if await self.should_compress(messages):
            compressed = await self.compress(messages)
            self._cache[session_id] = compressed
```

**与 EventBus 的关系**：会话更新时触发预压缩检查，而非每次 LLM 调用前同步判断。

**性能影响**：

| 层级 | 延迟 | Token 节省 | 适用场景 |
|------|------|-----------|----------|
| 消息剪枝 | 0ms | ~10-20% | 始终生效 |
| 摘要压缩 | ~500-1000ms | ~50-70% | 对话 > 20 轮 |
| 后台预压缩 | 0ms（异步） | ~50-70% | 长对话场景 |

**待办事项**：

- [ ] 实现消息剪枝（去重、合并、截断）
- [ ] 实现摘要压缩（LLM 生成历史摘要）
- [ ] Token 预算估算与触发阈值配置
- [ ] 后台预压缩机制（EventBus 订阅 + 缓存）
- [ ] 与现有 Chat/RAG 流程集成（AgentService / unified_chat）
- [ ] 前端显示压缩提示（如"已压缩 15 条历史消息"）

---

## 二、Phase 2 — 智能化升级

> 目标：构建 PaperChat 的核心差异化能力——多 Agent 协作研究助手。

### 2.1 多 Agent 研究助手（核心功能）

**为什么需要多 Agent**：研究类问题（如"我在做图像超分，怎样提升性能？"）需要**检索 → 分析 → 推荐**多个专业步骤并行协作，单 Agent 线性执行效率低且容易丢失上下文。

**架构**：

```
┌──────────────────────────────────────────────────┐
│              Orchestrator（协调器）                │
│         任务分解 · 调度 · 结果整合                │
└──────────┬──────────┬──────────┬─────────────────┘
           │          │          │
     ┌─────▼──┐ ┌─────▼──┐ ┌────▼─────┐
     │Retriever│ │Analyzer│ │Recommender│
     │ 检索专家 │ │分析专家 │ │ 推荐专家  │
     └────────┘ └────────┘ └──────────┘
```

| Agent | 职责 | 说明 |
|-------|------|------|
| **Orchestrator** | 任务分解、调度、结果整合 | 唯一面向用户的入口 |
| **Retriever** | 检索相关论文、提取关键信息 | 调用 RAG + BM25 |
| **Analyzer** | 分析论文方法、对比实验结果 | 调用 LLM 做深度分析 |
| **Recommender** | 生成研究方向/改进方案推荐 | 基于分析结果推理 |

**关键设计决策**：

- Orchestrator 是唯一对外接口，用户只与它交互
- 子 Agent 之间不直接通信，全部通过 Orchestrator 协调
- 每个 Agent 共享同一套工具集，但系统提示词不同（角色不同）
- 前端只展示 Orchestrator 的输出流，子 Agent 过程作为可展开的详情

**与 Chat 统一入口的关系**：研究助手是 Chat 意图路由的一个分支。当 Agent 判断用户问题属于"研究类"（复杂度高、需要多步推理），自动切换到多 Agent 模式。

---

#### 2.1.1 详细实现方案

##### 数据模型：Agent 间通信协议

```python
# backend/app/services/agent/research_types.py

from dataclasses import dataclass, field
from typing import Optional, List, Dict, Any


@dataclass
class SubAgentStep:
    """子 Agent 的单个 ReAct 步骤，用于前端展示"""
    role: str                    # "retriever" | "analyzer" | "recommender"
    step: int                    # 步骤编号
    type: str                    # "agent_thought" | "agent_action" | "agent_observation"
    content: str = ""
    tool: str = ""
    tool_input: dict = field(default_factory=dict)


@dataclass
class SubAgentResult:
    """子 Agent 执行结果"""
    role: str
    status: str                  # "success" | "error" | "skipped"
    summary: str                 # 结果摘要（供 Orchestrator 合成）
    raw_data: dict = field(default_factory=dict)
    steps: List[SubAgentStep] = field(default_factory=list)
    error_message: str = ""


@dataclass
class ResearchTask:
    """Orchestrator 分配给子 Agent 的任务"""
    task_id: str
    agent_role: str              # "retriever" | "analyzer" | "recommender"
    query: str                   # 子任务问题（从用户原问题分解得出）
    context: dict = field(default_factory=dict)  # 共享上下文（论文列表、历史结果）
    tools: List[str] = field(default_factory=list)  # 允许使用的工具名列表


@dataclass
class ResearchPlan:
    """Orchestrator 的任务分解结果"""
    original_query: str
    tasks: List[ResearchTask] = field(default_factory=list)
    execution_order: List[str] = field(default_factory=list)  # task_id 执行顺序
    parallel_groups: List[List[str]] = field(default_factory=list)  # 可并行的 task_id 组
```

##### 文件清单

| 文件 | 操作 | 说明 |
|------|------|------|
| `services/agent/research_types.py` | **新建** | 通信协议数据类 |
| `services/agent/coordinator.py` | **新建** | Orchestrator 协调器 |
| `services/agent/research_agents.py` | **新建** | Retriever/Analyzer/Recommender 子 Agent 工厂 |
| `prompts/research.py` | **新建** | 各角色系统提示词 |
| `handlers/unified_handler.py` | **修改** | 集成多 Agent 路由 |
| `frontend/src/hooks/useChatMessages.js` | **修改** | 订阅多 Agent 事件 |
| `frontend/src/stores/messageStore.ts` | **修改** | 扩展 agentSteps 类型 |
| `frontend/src/components/ChatPage/AgentProgress.jsx` | **修改** | 支持多 Agent 层级展示 |
| `frontend/src/pages/ChatPage.jsx` | **修改** | 接入多 Agent 进度 |

##### 详细设计

**1. 子 Agent 工厂 (`research_agents.py`)**

每个子 Agent 是 `ReActAgent` 实例 + 角色特定配置：

| 子 Agent | 系统提示词 | 可用工具 | max_iterations |
|----------|-----------|---------|---------------|
| **Retriever** | 检索专家 — 专注于搜索和提取论文信息 | SearchTextTool, SearchPapersTool, GetPaperInfoTool, ExtractKeyPointsTool, SearchCardsTool | 3 |
| **Analyzer** | 分析专家 — 专注于深度分析和对比 | SummarizeTool, CompareContentTool, AssessQualityTool, ExplainTermTool, TranslateTool | 4 |
| **Recommender** | 推荐专家 — 基于证据提出研究方向 | LiteratureReviewTool, GenerateOutlineTool, RecentPapersTool | 3 |

实现要点：
- 每个子 Agent 从 `react_agent.run(query, ctx, system_prompt_override, tool_subset)` 创建
- `ReActAgent.run()` 需增加 `system_prompt_override` 和 `tool_subset` 参数
- 子 Agent 返回 `SubAgentResult`，包含所有步骤供前端展示

**2. Orchestrator 协调器 (`coordinator.py`)**

```python
class ResearchCoordinator:
    """
    多 Agent 研究助手协调器
    
    流程：
    1. analyze_task() — LLM 分析用户问题，拆分为子任务
    2. dispatch() — 按依赖关系派发（可并行则并行）
    3. synthesize() — LLM 整合各子 Agent 结果
    """
    
    async def run(self, query: str, ctx: ToolContext, chat_history: str = "") -> AsyncGenerator[dict, None]:
        """
        主入口，yield 事件给 unified_handler
        
        Yield 事件类型:
        - "research_plan": 任务分解计划（前端展示）
        - "sub_agent_start": 子 Agent 开始执行
        - "sub_agent_step": 子 Agent 中间步骤
        - "sub_agent_done": 子 Agent 完成
        - "research_synthesis": Orchestrator 整合中
        - "research_result": 最终结果（流式文本）
        """
```

任务分解策略（LLM-driven）：

```
System: 你是一个研究任务规划专家。将用户的研究问题分解为多个子任务。
每个子任务指定角色(retriever/analyzer/recommender)和具体查询。

用户问题: {query}
已有上下文: {context}

请输出 JSON:
{
  "tasks": [
    {"role": "retriever", "query": "...", "depends_on": []},
    {"role": "analyzer", "query": "...", "depends_on": ["task_0"]},
    {"role": "recommender", "query": "...", "depends_on": ["task_1"]}
  ]
}
```

依赖关系图：
- `retriever` — 无依赖，最先执行（可多个并行）
- `analyzer` — 依赖 retriever 结果（可多个并行）
- `recommender` — 依赖 analyzer 结果（通常只有一个）

**3. 集成到 unified_handler (`unified_handler.py`)**

在 `_is_deep_research_request()` 旁新增路由：

```python
# unified_handler.py 新增函数
async def _handle_multi_agent_research(websocket, db, state, message, session_id, user_id,
                                        paper_id, paper_ids, chat_history, thinking_mode):
    """多 Agent 研究助手入口"""
    from app.services.agent.coordinator import ResearchCoordinator
    
    coordinator = ResearchCoordinator()
    ctx = ToolContext(db=db, paper_id=paper_id, paper_ids=paper_ids,
                      user_id=user_id, session_id=session_id)
    
    async for event in coordinator.run(query=message, ctx=ctx, chat_history=chat_history):
        event_type = event["type"]
        
        if event_type == "research_plan":
            # 发送任务分解计划给前端
            await websocket.send_text(json.dumps({
                "type": "research_plan",
                "tasks": event["tasks"]
            }))
            
        elif event_type == "sub_agent_start":
            await websocket.send_text(json.dumps({
                "type": "sub_agent_start",
                "role": event["role"],
                "query": event["query"]
            }))
            
        elif event_type == "sub_agent_step":
            await websocket.send_text(json.dumps({
                "type": "sub_agent_step",
                "role": event["role"],
                "step": event["step"],
                "sub_type": event["sub_type"],  # thought/action/observation
                "content": event.get("content", ""),
                "tool": event.get("tool", ""),
                "tool_input": event.get("tool_input", {})
            }))
            
        elif event_type == "sub_agent_done":
            await websocket.send_text(json.dumps({
                "type": "sub_agent_done",
                "role": event["role"],
                "summary": event["summary"]
            }))
            
        elif event_type == "research_synthesis":
            await websocket.send_text(json.dumps({
                "type": "research_synthesis",
                "content": "正在整合各专家分析结果..."
            }))
            
        elif event_type == "research_result":
            # 流式发送最终结果
            await websocket.send_text(json.dumps({
                "type": "rag_chat_chunk",
                "content": event["content"]
            }))
    
    # 发送完成信号
    await websocket.send_text(json.dumps({
        "type": "done",
        "channel": "multi_agent_research",
        "session_id": session_id
    }))
```

在 `handle_unified_chat()` 中注册路由（约 L793 附近）：

```python
# 在 _is_deep_research_request() 判断后，新增多 Agent 分支
if _is_deep_research_request(message, intent_result):
    thinking_mode = data.get("thinking_mode", "deep")
    # 高复杂度 → 多 Agent 模式
    if _is_multi_agent_research(message):
        async with AsyncSessionLocal() as db:
            await _handle_multi_agent_research(
                websocket, db, state, message, session_id, user_id,
                paper_id, paper_ids, chat_history, thinking_mode
            )
    else:
        # 中低复杂度 → 单 ReAct Agent
        async with AsyncSessionLocal() as db:
            await _handle_react_agent(websocket, db, state, message, ...)
    return
```

`_is_multi_agent_research()` 判定条件：
- 问题包含 "对比分析"、"综述"、"研究方向"、"进展"、"趋势" 等关键词
- LLM 意图识别结果为 `detect_contradiction`、`trace_evolution`、`find_research_gaps`、`cross_paper_reason`
- 包含多个论文 ID（>3）的查询

**4. ReActAgent 改造 (`react_agent.py`)**

`run()` 方法增加两个可选参数：

```python
async def run(self, query: str, ctx: ToolContext,
              chat_history: str = "",
              thinking_mode: str = "quick",
              mode: str = "normal",
              system_prompt_override: str = None,    # 新增
              tool_subset: List[str] = None) -> AsyncGenerator[dict, None]:  # 新增
    """
    Args:
        system_prompt_override: 覆盖默认系统提示词（子 Agent 使用）
        tool_subset: 限制可用工具列表（子 Agent 使用），None=全部工具
    """
```

改动量：
- `_build_prompt()` 中：`system_prompt_override` 不为空时替换默认 system prompt
- `_build_prompt()` 中：`tool_subset` 不为空时过滤 `tools_description`
- 工具执行时：`tool_subset` 不为空且工具不在列表中时跳过

**5. 角色提示词 (`prompts/research.py`)**

```
RETRIEVER_SYSTEM_PROMPT = """你是一个学术检索专家。你的职责是：
1. 搜索与用户问题相关的论文和资料
2. 提取论文的关键信息（方法、结果、贡献）
3. 整理检索结果为结构化的摘要

可用工具：搜索论文、获取论文详情、提取关键点、搜索知识卡片
约束：只做检索和提取，不做深度分析，不生成推荐。"""

ANALYZER_SYSTEM_PROMPT = """你是一个学术分析专家。你的职责是：
1. 对检索到的论文进行深度分析
2. 对比不同论文的方法和实验结果
3. 评估论文质量和创新点
4. 发现研究空白和矛盾之处

可用工具：总结、对比分析、质量评估、术语解释、翻译
约束：基于已有资料分析，不做新的检索，不生成推荐。"""

RECOMMENDER_SYSTEM_PROMPT = """你是一个研究方向推荐专家。你的职责是：
1. 基于分析结果，总结研究现状
2. 识别改进空间和未解决问题
3. 生成具体的研究方向或改进方案建议
4. 为每个建议提供证据支撑

可用工具：生成综述、获取近期论文
约束：推荐必须基于检索和分析的结果，不能凭空建议。"""
```

**6. 前端展示 (`AgentProgress.jsx`)**

扩展 `StepItem` 组件，新增层级展示：

```
┌──────────────────────────────────────────────┐
│  🔬 多 Agent 研究助手 (3 个专家)              │
├──────────────────────────────────────────────┤
│                                              │
│  ┌─ 🔍 检索专家 (已完成) ─────────────────┐  │
│  │  💭 思考: 需要搜索超分辨率相关论文...   │  │
│  │  🔧 SearchPapersTool                    │  │
│  │  👁 观察: 找到 5 篇相关论文             │  │
│  └────────────────────────────────────────┘  │
│                                              │
│  ┌─ 📊 分析专家 (进行中) ─────────────────┐  │
│  │  💭 思考: 对比各方法的性能指标...       │  │
│  │  🔧  ● ● ●                             │  │
│  └────────────────────────────────────────┘  │
│                                              │
│  ┌─ 💡 推荐专家 (等待中) ─────────────────┐  │
│  │  ⏳ 等待分析结果...                     │  │
│  └────────────────────────────────────────┘  │
│                                              │
│  🔄 正在整合各专家结果...                    │
└──────────────────────────────────────────────┘
```

##### 实现顺序（建议 3-4 天）

| 步骤 | 内容 | 文件 | 预估 |
|------|------|------|------|
| 1 | 定义通信协议数据类 | `research_types.py` | 0.5h |
| 2 | 实现角色提示词 | `prompts/research.py` | 1h |
| 3 | ReActAgent 增加 system_prompt_override + tool_subset | `react_agent.py` | 1h |
| 4 | 实现子 Agent 工厂 | `research_agents.py` | 1h |
| 5 | 实现 Orchestrator 协调器 | `coordinator.py` | 3h |
| 6 | 集成到 unified_handler | `unified_handler.py` | 1h |
| 7 | 前端订阅新事件 | `useChatMessages.js`, `messageStore.ts` | 0.5h |
| 8 | 前端层级展示 | `AgentProgress.jsx`, `ChatPage.jsx` | 2h |
| 9 | 端到端测试 | — | 1h |

**总计：约 11h（2 个工作日）**

---

### 2.1-B 简报生成 Agent（Briefing Mode）

> 简报模式是多 Agent 研究助手的一个专门模式，负责按用户设定的频率自动生成结构化学术简报——用户说"每天给我推送相关论文"，Agent 自动完成检索、筛选、摘要、排序全流程。

**核心流程**：

```
用户: "每天给我推送计算机视觉方向的最新论文"
  │
  ▼
简报生成 Agent 工作流:
  │
  ├── 1. 读取用户画像（阅读历史、标注偏好、搜索关键词）
  ├── 2. 调用 Phase 4 的多源搜索集群检索最新论文
  ├── 3. 使用本地 LLM 生成摘要和个性化排序
  ├── 4. 输出 HTML/Markdown 格式简报，在软件内 WebView 展示
  └── 5. 用户可通过自然语言修改简报模板（"简报里加一个'高被引论文'板块"）
```

**简报数据模型**：

```python
class BriefingSubscription(Base):
    user_id = Column(Integer, ForeignKey("users.id"))
    frequency = Column(String)       # "daily" | "weekly"
    keywords = Column(JSON)          # 用户关注的关键词/领域
    template_config = Column(JSON)   # 简报模板配置（板块、排序、格式）
    last_generated = Column(DateTime)

class BriefingHistory(Base):
    subscription_id = Column(Integer, ForeignKey("briefing_subscriptions.id"))
    generated_at = Column(DateTime)
    content_html = Column(Text)      # 生成的简报 HTML
    paper_ids = Column(JSON)         # 包含的论文 ID 列表
```

**简报生成流水线**：

```
搜索 → 筛选 → 摘要 → 渲染
  │       │       │       │
  ▼       ▼       ▼       ▼
多源搜索  去重+   本地LLM  HTML/Markdown
集群检索  相关性   生成     模板渲染
          排序    摘要     WebView展示
```

> **依赖说明**：简报生成的"多源搜索集群检索"步骤依赖 Phase 4.2 的 SearchDispatcher 和 MCP Server 集群。在 Phase 4 完成前，简报生成可先使用本地 RAG 检索作为降级方案，Phase 4 完成后切换为多源搜索。

**待办事项**：

- [ ] 设计简报数据模型（用户订阅、简报历史、模板配置）
- [ ] 实现简报生成流水线（搜索 → 筛选 → 摘要 → 渲染）
- [ ] 集成 WebView 组件用于内部打开论文链接
- [ ] 用户画像读取与个性化排序逻辑
- [ ] 自然语言修改简报模板（Agent 解析用户指令动态调整模板）

---

### 2.2 知识图谱自动构建

**现状**：KnowledgeGraphPage 存在但功能待确认。

**实现思路**：

```
论文上传 → NER 提取实体 → 匹配已有节点 → 创建/更新关系 → 渲染图谱
```

**是否需要多 Agent**：**不需要**。这是一个确定性的数据处理流水线，每一步的输入输出都是明确的，不需要 LLM 自主决策。用普通的异步任务队列即可：

```python
async def build_knowledge_graph(paper_id: int):
    entities = await ner_service.extract(paper_id)
    matched = await graph_service.match_entities(entities)
    await graph_service.update_graph(matched)
```

**与 Chat 的关系**：图谱构建在论文上传后自动触发。用户可以在 Chat 中查询图谱信息（如"这篇论文和哪些论文有关联"），但构建过程本身不需要 Chat 介入。

**待办事项**：

- [ ] 检查现有知识图谱实现程度
- [ ] 设计知识图谱数据模型（节点、边、属性）
- [ ] 实现 NER 实体提取（可调用 LLM，但作为工具函数而非 Agent）
- [ ] 实现实体匹配与关系构建
- [ ] 图谱可视化渲染（D3.js / vis-network）

---

## 三、Phase 3 — 体验打磨

> 目标：提升交互流畅度和智能化感知。

### 3.1 Pretext 文本布局引擎集成

| 项目 | 说明 |
|------|------|
| 库 | `@chenglou/pretext`（15KB，无 DOM 文本测量） |
| 核心能力 | 微秒级文本高度计算、缓存机制 |

**应用场景**：

| 场景 | 说明 | 优先级 |
|------|------|--------|
| 打字机效果优化 | 预计算最终高度，消除气泡跳动 | **高** |
| 虚拟列表高度缓存 | 论文列表、会话列表预计算高度 | **高** |
| 文本展开/收起动画 | 摘要、分析结果平滑展开 | 中 |
| 聊天气泡高度预计算 | 优化滚动定位 | 中 |

**待办事项**：

- [ ] 安装 `@chenglou/pretext`
- [ ] 封装 `useTextLayout` Hook
- [ ] ChatPanel 打字机效果优化
- [ ] PaperList / SessionList 虚拟列表集成

---

### 3.2 推荐系统增强

**现状**：`recommendation_service.py` 已有基础推荐逻辑。

**增强方向**：

| 功能 | 实现方式 | 是否需要 Agent |
|------|----------|----------------|
| 基于阅读历史的推荐 | 协同过滤 / 内容相似度 | ❌ 算法逻辑 |
| 基于知识图谱的推荐 | 图谱路径发现 | ❌ 图算法 |
| 个性化排序 | 用户画像 + 权重 | ❌ 机器学习 |

**不需要 Agent**：推荐是算法问题，不是 LLM 规划问题。Agent 在这里只会增加延迟和成本。

**与 Chat 的关系**：用户在 Chat 中说"推荐我可能感兴趣的论文"时，通过 `search_papers` 工具调用推荐服务。

**待办事项**：

- [ ] 完善用户画像（阅读时长、标注偏好、研究主题）
- [ ] 实现基于内容的相似论文推荐
- [ ] 首页推荐卡片展示

---

### 3.3 批量论文分析

**现状**：支持批量导入，但无批量分析。

**实现方式**：异步任务队列 + 进度条，不需要 Agent。

```python
async def batch_analyze(paper_ids: List[int]):
    for paper_id in paper_ids:
        await analyze_service.analyze(paper_id)
        await progress_callback(len(done), len(total))
```

**与 Chat 的关系**：用户在 Chat 中说"帮我分析这 10 篇论文"时触发，进度通过 Chat 消息推送。

**待办事项**：

- [ ] 后端批量分析 API（异步任务 + 进度推送）
- [ ] 前端批量分析入口 + 进度展示
- [ ] 分析结果汇总报告生成

---

### 3.4 用户控制与成本感知面板

> 提升用户体验和信任感的关键功能——让用户对模型选择、费用、隐私有完全的控制权和可见性。

| 功能 | 说明 | 实现方式 |
|------|------|----------|
| **模型选择器** | Chat 界面下拉菜单（本地优先 / 智能路由 / 云端优先） | 前端组件 + 后端偏好存储 |
| **费用确认弹窗** | 当 Agent 判断需调用云端且预估费用超过阈值时，询问用户 | 复用 Phase 3.5 的错误码和确认对话框 |
| **预算仪表盘** | 设置页面展示本月云端费用、各服务免费额度剩余 | 后端统计 + 前端图表 |
| **实时模型指示器** | 每条消息旁显示所用模型图标（☁️/🏠）和费用 | WebSocket 推送元数据 |

**用户偏好数据模型**：

```python
class UserPreferences(Base):
    user_id = Column(Integer, ForeignKey("users.id"), unique=True)
    model_mode = Column(String, default="smart_route")  # "local_only" | "smart_route" | "cloud_only"
    budget_limit = Column(Float, default=10.0)           # 月度预算上限（元）
    confirm_threshold = Column(Float, default=0.5)       # 单次确认阈值（元）
    anonymous_mode = Column(Boolean, default=False)      # 匿名使用模式
```

**费用预估服务**：

```python
class CostEstimator:
    async def estimate(self, task: Task, backend: InferenceBackend) -> CostEstimate:
        if backend.is_local:
            return CostEstimate(cost=0.0, currency="CNY", note="本地推理，无费用")
        token_count = self._estimate_tokens(task)
        price_per_1k = PRICING_TABLE[backend.model_name]
        estimated_cost = token_count / 1000 * price_per_1k
        return CostEstimate(cost=estimated_cost, currency="CNY")
```

**待办事项**：

- [ ] 用户偏好 API（存储 model_mode、budget_limit、confirm_threshold）
- [ ] 费用预估服务（根据任务类型和模型价格计算）
- [ ] 前端模型选择器组件
- [ ] 预算仪表盘页面
- [ ] 实时模型指示器（WebSocket 推送元数据到每条消息）

---

### 3.5 内嵌 WebView 组件

> 提升沉浸感——用户在 Chat 中点击论文链接时，不需要跳转到外部浏览器，直接在软件内部打开网页查看。

**实现方案**：

| 方案 | 说明 | 适用场景 |
|------|------|----------|
| Web 版 | 使用 iframe 或 react-webview 实现简单版本 | Phase 3 先行 |
| 桌面端 | 集成 Tauri WebView（Phase 8 已规划） | Phase 8 完善 |

**功能清单**：

| 功能 | 说明 |
|------|------|
| 内部打开链接 | Chat 消息中每个链接旁增加"在内部打开"按钮 |
| 基本浏览器控制 | 返回上一页、刷新、前进 |
| 简报展示 | Phase 2 简报模式的 HTML/Markdown 内容在此 WebView 中渲染 |

**待办事项**：

- [ ] Web 版 iframe/react-webview 组件实现
- [ ] Chat 消息链接"在内部打开"按钮
- [ ] 基本浏览器控制栏（返回/刷新/前进）

---

### 3.6 启动性能优化

**现状**：`lifespan()` 中所有初始化串行执行——数据库连接 → 加载设置 → RAG/LLM → Agent → ToolRegistry → MCP → Skills → 事件订阅。全部完成后应用才响应第一个请求。随着服务增多，启动时间线性增长。

**方案**：分级启动 + 并行初始化 + 懒加载

```
启动流程优化后:

Phase 0 — 模块导入阶段（同步）
  ├── 设置 HuggingFace 镜像、缓存路径
  └── 导入核心依赖

Phase 1 — 关键路径（串行，必须就绪才能接受请求）
  ├── 初始化数据库连接 ← 必须
  ├── 创建引擎 & 会话工厂
  ├── 创建表
  └── 确保默认用户存在

Phase 2 — 后台并行（asyncio.gather，不阻塞启动）
  ├── 加载用户设置 ────┐
  ├── 初始化 RAG 服务 ├──→ asyncio.gather()
  ├── 初始化 MCP Manager│
  ├── 注册 ToolRegistry ┘
  └── 初始化 EventBus、注册订阅者

Phase 3 — 懒加载（首次使用时才初始化）
  ├── LLM 服务 → 首次用户消息时初始化
  ├── ChromaDB 索引 → 首次检索时加载
  ├── Embedding 模型 → 首次生成 embedding 时加载
  └── Agent 工具 → 首次意图识别时注册
```

**技术实现**：

```python
# bootstrap/startup.py — 启动管理器
class StartupManager:
    def __init__(self):
        self._ready = asyncio.Event()
        self._services = {}
        self._timings = {}
        self._background_tasks = []

    async def startup(self, app):
        """分阶段启动"""
        startup_profile = {}

        # Phase 1: 关键路径（串行）
        t0 = perf_counter()
        await self._init_database(app)
        startup_profile['database'] = perf_counter() - t0

        # 标记服务就绪，开始接受请求
        app.state.startup_ready = True
        self._ready.set()

        # Phase 2: 后台并行
        t0 = perf_counter()
        tasks = [
            self._init_settings(app),
            self._init_rag(app),
            self._init_mcp(app),
            self._init_tools(app),
            self._init_eventbus(app),
        ]
        await asyncio.gather(*tasks)
        startup_profile['background'] = perf_counter() - t0

        # Phase 3: 启动 profiling（供优化参考）
        log_startup_profile(startup_profile)

    async def get_service(self, name: str, init_fn):
        """懒加载服务"""
        if name not in self._services:
            self._services[name] = await init_fn()
        return self._services[name]

    @property
    def is_ready(self):
        return self._ready.is_set()
```

**启动 Profiling**：

```python
def log_startup_profile(profile: dict):
    """记录各阶段耗时，供后续优化参考"""
    logger.info(f"启动 Profile: DB={profile['database']:.2f}s | "
                f"后台={profile['background']:.2f}s | "
                f"总计={sum(profile.values()):.2f}s")
```

**性能预期**：

| 优化项 | 当前 | 优化后 | 提升 |
|--------|------|--------|------|
| 首字节时间（TTFB） | 等待全部初始化完成 | Phase 1 后即响应 | 减少 60-80% |
| 完全就绪时间 | 3-8s | 5-10s（但用户无感知） | 感知启动速度大幅提升 |
| 内存占用 | 启动时加载全部 | 按需加载 | 减少 30-50% |

**待办事项**：

- [ ] 实现 StartupManager 分阶段启动
- [ ] 重写 lifespan() 为 Phase 1 + Phase 2 结构
- [ ] 关键服务懒加载改造（LLM、Embedding、ChromaDB）
- [ ] 启动 Profiling 日志
- [ ] 验证启动时间提升

---

## 三-B、Phase 3.5 — 工程规范化基础设施（与 Phase 3 并行）

> 目标：在功能选代的同时补齐工程规范性短板，为 Phase 4 的 MCP 工具库和 ConfigAgent 打下坚实基础。

### 3.5.1 提示词版本管理

**现状**：`services/llm/prompts/` 已完成提示词分离，但缺乏版本追踪和 A/B 测试能力。

**需要做的事**：

| 事项 | 说明 |
|------|------|
| 提示词版本号标注 | 每个提示词文件头部添加 `# Version: X.Y` 和变更日志 |
| 提示词注册表 | `prompts/__init__.py` 记录每个提示词的版本和最后修改日期 |
| 提示词效果追踪 | 记录每次 LLM 调用使用的提示词版本，支持回滚对比 |

**性能影响**：无运行时开销，仅在 LLM 调用时附加一个版本号元数据字段。

---

### 3.5.2 结构化错误码体系

**现状**：后端错误信息为自由文本字符串，前端无法按错误类型做差异化处理。

**设计方案**：

```python
# 错误码格式：ERR-XXXX（4位数字，前2位为模块，后2位为序号）
ERR_CODE = {
    # 10xx — 通用错误
    "ERR-1001": "参数校验失败",
    "ERR-1002": "资源未找到",
    "ERR-1003": "权限不足",
    # 20xx — 论文相关
    "ERR-2001": "PDF 解析失败",
    "ERR-2002": "论文已存在",
    "ERR-2003": "索引构建失败",
    # 30xx — RAG/LLM 相关
    "ERR-3001": "LLM 服务不可用",
    "ERR-3002": "RAG 检索超时",
    "ERR-3003": "Token 超限",
    # 40xx — Agent/MCP 相关
    "ERR-4001": "Agent 工具执行失败",
    "ERR-4002": "MCP Server 连接超时",
    "ERR-4003": "MCP 工具未注册",
    # 50xx — 认证/配置相关
    "ERR-5001": "API Key 无效",
    "ERR-5002": "WebSocket 认证失败",
    "ERR-5003": "配置同步失败",
}
```

**前端对接**：`api/index.ts` 拦截器根据 `err_code` 字段做差异化 Toast 和重试策略。

**性能影响**：可忽略，仅增加一个字符串常量的返回。

---

### 3.5.3 工具权限分级控制

**现状**：Agent 工具无权限控制，所有工具对所有用户一视同仁。

**设计方案**：

| 权限级别 | 说明 | 典型工具 |
|----------|------|----------|
| `public` | 无需认证即可使用 | `search_text`、`get_paper_info` |
| `authenticated` | 需登录用户 | `summarize`、`translate`、`explain_term` |
| `admin` | 仅管理员 | `assess_quality`、`config_service` |

```python
class ToolBase:
    permission_level: str = "authenticated"  # 默认需登录

    async def execute(self, context: ToolContext):
        if not self._check_permission(context):
            raise PermissionError(f"工具 {self.name} 需要更高权限")
        return await self._execute(context)
```

**性能影响**：每次工具调用增加一次权限检查（O(1) 内存查表），开销可忽略。

---

### 3.5.4 Prettier + Pre-commit 代码风格统一

**现状**：前后端代码风格不统一，无自动格式化。

**方案**：

| 工具 | 适用范围 | 配置文件 |
|------|----------|----------|
| Prettier | 前端 `.js/.jsx/.ts/.tsx/.css` | `.prettierrc` |
| Black + isort | 后端 `.py` | `pyproject.toml` |
| pre-commit | 全仓库 | `.pre-commit-config.yaml` |

**待办事项**：

- [ ] 添加 `.prettierrc` 和 `.prettierignore`
- [ ] 在 `pyproject.toml` 中配置 `[tool.black]` 和 `[tool.isort]`
- [ ] 添加 `.pre-commit-config.yaml`（Prettier + Black + isort + trailing-whitespace）
- [ ] 初始化 pre-commit：`pre-commit install`

**性能影响**：提交时增加 ~2-5s 格式化检查，不影响运行时性能。

---

### 3.5.5 TypeScript strict 迁移

**现状**：Store 和 API 层已迁移为 `.ts`，但组件仍为 `.jsx`，`tsconfig.json` 未启用 `strict`。

**迁移路线**：

```
当前 tsconfig: strict: false
  ↓
渐进式启用：
  1. strictNullChecks → 消除 null/undefined 隐患
  2. noImplicitAny → 为所有隐式 any 添加类型
  3. strictFunctionTypes → 函数签名精确化
  ↓
目标 tsconfig: strict: true
```

**性能影响**：编译时间增加 ~10-20%（类型检查开销），运行时无影响。

---

### 3.5.6 核心路径测试覆盖

**现状**：仅有 2 个测试文件，覆盖率极低。

**目标**：核心路径测试覆盖率 ≥ 60%

| 优先测试模块 | 说明 | 目标覆盖率 |
|-------------|------|------------|
| RAG 服务 | 检索 + 生成核心链路 | 80% |
| Agent 工具 | 9+ 工具执行逻辑 | 70% |
| 消息/会话服务 | CRUD + 缓存 | 80% |
| WebSocket Handler | 消息路由 + 流式推送 | 60% |
| LLM 服务 | 提示词构建 + 调用 | 70% |

**待办事项**：

- [ ] 搭建后端 pytest 测试框架（conftest.py + fixtures）
- [ ] 搭建前端 vitest 测试框架
- [ ] 编写核心服务单元测试
- [ ] 配置 CI 测试流水线

**性能影响**：测试运行时增加 ~30s，不影响生产性能。单元测试在 CI 中自动运行，每次 PR 必须通过。

---

### 3.5.8 多级配置体系

**现状**：配置分散在三个位置——`config.py`（Pydantic Settings 系统级）、`settings_service.py`（数据库用户配置）、`settingsStore.ts`（前端内存）。三者之间无层级覆盖关系，也无变更同步机制。

**方案**：建立多级配置覆盖链，上层覆盖下层

```
配置覆盖链（优先级从高到低）:

┌─────────────────────────────────────┐
│  1. CLI 参数 / 环境变量              │  ← 最高优先级
│     如: DEEPSEEK_API_KEY=xxx         │     部署时注入
├─────────────────────────────────────┤
│  2. 用户配置（数据库）                │  ← 用户通过 Settings 页面设置
│     如: model_mode, budget_limit     │     持久化到 DB
├─────────────────────────────────────┤
│  3. 项目配置（可选）                  │  ← 项目级 .paperchat.yml
│     如: CORS_ORIGINS, 默认模型       │     团队共享
├─────────────────────────────────────┤
│  4. 系统配置（config.py / .env）     │  ← 最低优先级
│     如: DATABASE_URL, DEBUG          │     全局默认值
└─────────────────────────────────────┘
```

**实现方案**：

```python
# services/config/config_service.py
from pydantic_settings import BaseSettings
from typing import Any, Optional

class ConfigService:
    """多级配置服务"""

    def __init__(self):
        self._layers = ['cli', 'user', 'project', 'system']
        self._cached = {}
        self._listeners = []

    async def get(self, key: str, user_id: Optional[int] = None) -> Any:
        """按优先级顺序查找配置值"""
        # 1. CLI / 环境变量
        val = os.environ.get(key)
        if val is not None:
            return val

        # 2. 用户配置（数据库）
        if user_id:
            val = await self._get_user_setting(user_id, key)
            if val is not None:
                return val

        # 3. 项目配置（文件）
        val = self._get_project_config(key)
        if val is not None:
            return val

        # 4. 系统默认（Pydantic Settings）
        return getattr(settings, key, None)

    async def set(self, key: str, value: Any, user_id: int):
        """写入用户配置并发布变更事件"""
        await self._save_user_setting(user_id, key, value)
        await event_bus.publish(Event(
            type=EventTypes.SETTINGS_CHANGED,
            data={"user_id": user_id, "key": key, "value": value}
        ))

    def subscribe(self, listener):
        """订阅配置变更事件"""
        self._listeners.append(listener)

    def _get_project_config(self, key: str) -> Optional[Any]:
        """读取项目级 .paperchat.yml"""
        # 从项目根目录读取 YAML 配置
        ...
```

**配置变更同步机制**：

```
用户修改配置
  │
  ▼
ConfigService.set() 写入数据库
  │
  ▼
EventBus publish(SETTINGS_CHANGED)
  │
  ├── WebSocket 推送到前端
  │     └── settingsStore 自动更新
  │
  ├── LLM 服务重新加载模型配置
  └── RAG 服务更新检索参数
```

**迁移路线**：

1. 将现有 `settings_service.py` 整合到新的 `ConfigService`
2. 为所有配置读取路径添加层级覆盖逻辑
3. 引入项目级 `.paperchat.yml` 配置文件
4. 建立配置变更 WebSocket 推送机制
5. 前端 settingsStore 订阅 WebSocket 事件

**待办事项**：

- [ ] 实现 ConfigService 多级读取逻辑
- [ ] 整合现有 settings_service.py
- [ ] 项目级 .paperchat.yml 配置支持
- [ ] 配置变更事件发布 + WebSocket 推送
- [ ] 前端 settingsStore 订阅配置变更
- [ ] 迁移现有配置读取路径到 ConfigService

> 提升参赛作品的合规性和用户信任度——明确数据边界，提供安全审计和匿名使用能力。

| 规范项 | 说明 |
|--------|------|
| **数据最小化声明** | Settings 页面增加声明：明确哪些数据存储本地、哪些可能上传云端（如有），提供一键导出/删除所有用户数据的功能 |
| **API Key 安全审计日志** | 记录谁在什么时候使用了哪个 API Key（仅本地存储），便于用户审计 |
| **匿名使用模式** | 不发送任何遥测数据，所有推理和存储均在本地完成 |

**数据最小化声明示例**：

```
📋 PaperChat 数据最小化声明

本地存储（永不上传）：
  ✓ 论文 PDF 和解析数据
  ✓ 知识卡片和标注
  ✓ API Key（AES-256 加密）
  ✓ 对话历史和偏好设置

可能上传云端（仅在用户选择云端模型时）：
  △ 对话文本（发送至 DeepSeek API）
  △ 搜索查询（发送至 arXiv/Semantic Scholar API）

一键操作：
  [导出所有数据] [删除所有数据]
```

**待办事项**：

- [ ] Settings 页面增加数据最小化声明
- [ ] 一键导出/删除所有用户数据功能
- [ ] API Key 安全审计日志（本地存储）
- [ ] 匿名使用模式开关

---

## 四、Phase 4 — 自主配置 Agent + 外部服务集成（5-7 月）

> 目标：让 Agent 具备"自主配置外部服务"的能力——用户只需提供 API Key，Agent 自动完成连接、调试、调用全流程。
>
> 这是赛题的**核心创新点**——端侧 AI 智能体不只是被动回答问题，而是能主动扩展自身能力边界。先完成功能开发，再统一适配 AMD 平台。

### 4.1 ConfigAgent 架构

**核心理念**：传统应用需要用户手动配置 API 地址、参数、认证方式。ConfigAgent 让用户只需说"帮我连接 arXiv"并提供 API Key，Agent 自动完成一切。

```
用户: "帮我连接 arXiv，这是我的 API Key: xxx"
  │
  ▼
ConfigAgent 工作流:
  │
  ├── 1. 识别服务类型 → arXiv
  ├── 2. 查找服务注册表 → 获取 arXiv API 规范
  ├── 3. 验证 API Key → 发送测试请求
  ├── 4. 配置连接参数 → base_url, headers, rate_limit
  ├── 5. 展示配置预览 → "我将做以下配置：连接 arXiv，使用工具 search_arxiv 和 download_pdf，API 地址为...，是否继续？"
  ├── 6. 用户确认后才真正注册 MCP Server
  ├── 7. 注册可用工具 → SearchArxivTool, DownloadArxivTool
  └── 8. 通知用户 → "arXiv 已连接，你现在可以说'搜索关于 X 的论文'"
  │
  └── 如果失败 → 诊断错误 → 建议修复 → 重试
```

**ConfigAgent 与普通 Agent 的区别**：

| 维度 | 普通 Agent | ConfigAgent |
|------|-----------|-------------|
| 工具集 | 固定不变 | **动态扩展** |
| 能力边界 | 预定义 | **用户按需扩展** |
| 配置方式 | 开发者硬编码 | **用户自然语言 + API Key** |
| 错误处理 | 抛异常 | **自主诊断 + 修复建议** |

**服务注册表设计**：

```python
class ServiceRegistry:
    services = {
        "arxiv": {
            "name": "arXiv",
            "api_base": "http://export.arxiv.org/api/query",
            "auth_type": "none",
            "rate_limit": "1 req/s",
            "tools": ["search_arxiv", "download_arxiv"],
            "test_query": "search?search_query=all:electron&max_results=1",
        },
        "semantic_scholar": {
            "name": "Semantic Scholar",
            "api_base": "https://api.semanticscholar.org/graph/v1",
            "auth_type": "api_key",
            "header_name": "x-api-key",
            "rate_limit": "100 req/5min",
            "tools": ["search_s2", "get_paper_details", "get_citations"],
            "test_query": "paper/search?query=attention&limit=1",
        },
        "crossref": {
            "name": "CrossRef",
            "api_base": "https://api.crossref.org/works",
            "auth_type": "mailto",
            "rate_limit": "50 req/s",
            "tools": ["search_crossref", "get_doi_metadata"],
            "test_query": "?query=attention&rows=1",
        },
        "zotero": {
            "name": "Zotero",
            "api_base": "https://api.zotero.org",
            "auth_type": "api_key",
            "header_name": "Zotero-API-Key",
            "rate_limit": "100 req/min",
            "tools": ["sync_zotero_library", "import_zotero_papers"],
            "test_query": "users/{user_id}/items?limit=1",
        },
        "dblp": {
            "name": "DBLP",
            "api_base": "https://dblp.org/search/publ/api",
            "auth_type": "none",
            "rate_limit": "none",
            "tools": ["search_dblp"],
            "test_query": "?q=attention&h=1&format=json",
        },
    }
```

**待办事项**：

- [ ] ServiceRegistry 服务注册表实现
- [ ] ConfigAgent 核心逻辑（识别 → 查找 → 验证 → 配置 → 注册工具）
- [ ] 动态工具注册机制（运行时新增 Agent 工具）
- [ ] 连接诊断与自动修复
- [ ] Chat 中 ConfigAgent 意图路由
- [ ] ConfigAgent 生成人类可读的配置预览文本
- [ ] 前端展示确认对话框（复用 Phase 3.4 的组件）
- [ ] 用户确认后才真正注册 MCP Server

---

### 4.2 多源搜索服务集群

> 在原有学术源基础上扩展为多源搜索服务集群，引入通用网页搜索（Tavily、Bing、百度、Brave、wigolo），实现"平时网页搜索 + 论文查询"的完整覆盖，并支持智能轮询与额度追踪。

**学术搜索源**（原有）：

| 论文源 | 能力 | 认证方式 | 优先级 |
|--------|------|----------|--------|
| **arXiv** | 搜索、下载 PDF、元数据 | 无需 API Key | 🔴 高 |
| **Semantic Scholar** | 语义搜索、引用网络、影响力 | API Key（免费） | 🔴 高 |
| **CrossRef** | DOI 查询、引用元数据 | 邮箱（礼貌池） | 🟡 中 |
| **DBLP** | 计算机科学文献搜索 | 无需 API Key | 🟡 中 |
| **PubMed** | 生物医学文献 | API Key（免费） | 🟢 低 |
| **Google Scholar** | 通用学术搜索 | 无官方 API，需爬虫 | 🟢 低（风险高） |

**通用搜索源**（新增）：

| 搜索源 | 类型 | 免费额度 | 集成方式 |
|--------|------|----------|----------|
| **Tavily** | AI 优化搜索 | 1000 次/月 | 通过 MCP Server 封装 |
| **Bing Web Search** | 通用搜索 | 1000 次/月 | 通过 MCP Server 封装 |
| **百度搜索 API** | 中文搜索 | 1500 次/月 | 通过 MCP Server 封装 |
| **Brave Search** | 隐私搜索 | 2000 次/月 | 通过 MCP Server 封装 |
| **wigolo** | 本地免费搜索 | 无限 | 本地 MCP Server（零配置） |

**SearchDispatcher 智能调度器**：

```python
class SearchDispatcher:
    """智能调度多个搜索源，优先级排序、降级、额度追踪"""

    def __init__(self):
        self.sources: Dict[str, SearchSource] = {}
        self.usage_tracker = UsageTracker()  # 额度追踪

    async def search(self, query: str, mode: str = "auto") -> SearchResult:
        if mode == "academic":
            return await self._search_academic(query)
        elif mode == "web":
            return await self._search_web(query)
        else:
            return await self._search_auto(query)

    async def _search_auto(self, query: str) -> SearchResult:
        results = []
        for source in self._prioritize(query):
            if not self.usage_tracker.has_quota(source.name):
                continue
            try:
                result = await asyncio.wait_for(
                    source.search(query), timeout=5.0
                )
                results.append(result)
            except Exception:
                continue
        return self._merge_and_rank(results)
```

**ConfigAgent 自助配置**：用户在 Chat 中说"帮我配置 Tavily 搜索"并提供 API Key，ConfigAgent 自动注册对应的 MCP Server 并纳入 SearchDispatcher 调度。

**用户交互流程**：

```
用户: "最近 AI 领域有什么新闻"

Agent 判断: 需要通用网页搜索
  ├── 检查 SearchDispatcher 可用源
  ├── 优先使用 Tavily（AI 优化搜索，结果质量高）
  ├── Tavily 额度用尽 → 降级到 Bing → 百度
  ├── 如果用户未配置任何通用搜索源 → 提示:
  │   "我还没有连接通用搜索源。你可以：
  │    1. 说'连接 Tavily'（AI 优化搜索，1000 次/月免费）
  │    2. 说'连接 wigolo'（本地免费搜索，零配置）"
  └── 用户说"连接 wigolo" → ConfigAgent 自动配置 → 搜索新闻
```

**待办事项**：

- [ ] arXiv API 集成（搜索 + PDF 下载 + 元数据提取）
- [ ] Semantic Scholar API 集成（语义搜索 + 引用网络）
- [ ] CrossRef API 集成（DOI 查询）
- [ ] DBLP API 集成（CS 文献搜索）
- [ ] 论文源搜索结果统一格式化
- [ ] PDF 下载 → 自动入库 → 自动索引流水线
- [ ] 为每个通用搜索源实现对应的 MCP Server（或复用 arxiv-mcp-server 模式）
- [ ] 实现 SearchDispatcher 智能调度器（优先级、降级、额度追踪）
- [ ] 在 ConfigAgent 中增加对搜索源的自助配置（用户提供 API Key 后自动注册）

---

### 4.3 引用管理集成

| 系统 | 能力 | 认证方式 |
|------|------|----------|
| **Zotero** | 同步文献库、导入标注、导出引用 | API Key + User ID |
| **Mendeley** | 同步文献库、导入 PDF | OAuth 2.0 |

**ConfigAgent 自动配置流程（以 Zotero 为例）**：

```
用户: "帮我连接 Zotero"
  │
  ▼
ConfigAgent:
  1. "请提供你的 Zotero API Key 和 User ID"
     → 用户输入
  2. 发送测试请求: GET /users/{id}/items?limit=1
  3. 验证成功 → 注册 SyncZoteroTool, ImportZoteroTool
  4. "Zotero 已连接！你有 234 篇文献。要不要同步到 PaperChat？"
  5. 用户: "同步"
  6. 批量导入文献元数据 → 匹配已有论文 → 下载缺失 PDF
```

**待办事项**：

- [ ] Zotero API 集成（CRUD + 同步）
- [ ] Mendeley API 集成（OAuth + 同步）
- [ ] 文献库双向同步机制
- [ ] 引用格式自动转换（APA / IEEE / GB/T 7714）

---

### 4.4 API Key 安全管理

**安全设计**：

```
用户输入 API Key
  │
  ▼
加密存储（AES-256-GCM）
  │
  ├── 密钥派生: PBKDF2(用户密码, salt, 100000 iterations)
  ├── 存储: encrypted_key + iv + auth_tag + salt
  └── 使用时: 内存解密 → HTTP Header → 立即清除
```

| 安全措施 | 说明 |
|----------|------|
| **加密存储** | API Key 使用 AES-256-GCM 加密后存入数据库 |
| **内存安全** | 使用后立即清除明文，不写入日志 |
| **权限隔离** | API Key 仅 ConfigAgent 可访问，其他模块不可见 |
| **过期检测** | 定期检查 API Key 有效性，失效时通知用户 |
| **端侧优先** | API Key 存储在本地，不上传云端 |

**服务健康检测**：

```python
class ServiceHealthChecker:
    async def check(self, service_name: str) -> ServiceStatus:
        config = ServiceRegistry.get(service_name)
        try:
            response = await httpx.get(
                config.test_endpoint,
                headers=config.auth_headers,
                timeout=10
            )
            return ServiceStatus(
                name=service_name,
                available=True,
                latency_ms=response.elapsed_ms,
                rate_limit_remaining=response.headers.get("x-ratelimit-remaining"),
            )
        except Exception as e:
            return ServiceStatus(
                name=service_name,
                available=False,
                error=str(e),
                suggestion=self.diagnose(e),
            )
```

**待办事项**：

- [ ] API Key 加密存储模块
- [ ] ServiceHealthChecker 健康检测
- [ ] 定时健康检查 + 失效通知
- [ ] API Key 管理前端页面（Settings 中）
- [ ] 连接状态指示器（Chat 侧边栏）

---

### 4.5 ConfigAgent 功能验证与集成测试

**目标**：在云端环境下完成 ConfigAgent 的全部功能验证，确保后续 AMD 端侧适配时功能已完备。

| 验证项 | 说明 |
|--------|------|
| ConfigAgent 端到端流程 | 用户说"连接 arXiv" → 自动配置 → 可用 |
| 论文搜索 + 下载 | arXiv/Semantic Scholar 搜索 → PDF 下载 → 自动入库 |
| 引用管理同步 | Zotero 连接 → 文献同步 → 引用格式转换 |
| API Key 安全 | 加密存储 → 使用时解密 → 失效检测 |
| 服务降级 | 离线时 ConfigAgent 提示 → 网络恢复后自动执行 |

**待办事项**：

- [ ] ConfigAgent 端到端集成测试
- [ ] 论文源功能验证（arXiv + Semantic Scholar）
- [ ] 引用管理功能验证（Zotero）
- [ ] API Key 安全性审计
- [ ] 赛题 Demo 场景脚本编写

---

## 四-B、学术 MCP Server 工具库

> 目标：构建学术领域的 MCP (Model Context Protocol) 工具库，让 Agent 通过标准化协议调用外部学术服务，实现能力动态扩展。
>
> 与 ConfigAgent 的关系：ConfigAgent 负责“自主配置服务连接”，MCP Server 负责“标准化工具调用”。两者协同：ConfigAgent 配置连接 → MCP Manager 注册 Server → Agent 通过 MCP 协议调用工具。

### MCP 工具库架构

```
┌──────────────────────────────────────────────────┐
│              Agent Orchestrator                  │
│         意图识别 → 工具选择 → 结果整合             │
└────────────────────┬─────────────────────────────┘
                     │
                     ▼
┌──────────────────────────────────────────────────┐
│              MCP Manager（统一调度层）             │
│  Server 注册 · 路由 · 超时 · 降级 · 健康检测      │
└──────┬───────┬───────┬───────┬───────┬───────────┘
       │       │       │       │       │
  ┌────▼──┐┌───▼──┐┌──▼───┐┌──▼───┐┌──▼───┐┌────▼─────┐
  │arxiv  ││S2    ││cross ││dblp  ││zotero││report    │
  │server ││server││ref   ││server││server││generator │
  └───────┘└──────┘└──────┘└──────┘└──────┘└──────────┘
```

### 6 个学术 MCP Server 清单

| Server | 功能 | 核心工具 | 认证方式 | 优先级 |
|--------|------|----------|----------|--------|
| **arxiv-server** | 论文搜索与 PDF 下载 | `search_papers`、`download_pdf`、`get_metadata` | 无需 API Key | 🔴 高 |
| **semantic-scholar-server** | 语义搜索与引用网络 | `semantic_search`、`get_citations`、`get_references`、`get_influential_citations` | API Key（免费） | 🔴 高 |
| **crossref-server** | DOI 解析与元数据 | `resolve_doi`、`get_metadata`、`search_by_title` | 邮箱（礼貌池） | 🟡 中 |
| **dblp-server** | CS 领域文献检索 | `search_papers`、`get_author_papers`、`get_venue_papers` | 无需 API Key | 🟡 中 |
| **zotero-server** | 文献库双向同步 | `sync_library`、`import_papers`、`export_citations` | API Key + User ID | 🔴 高 |
| **report-generator-server** | 结构化报告生成 | `generate_outline`、`generate_literature_review`、`generate_comparison` | 内部服务 | 🔴 高 |

> **通用搜索源说明**：Phase 4.2 中的通用搜索源（Tavily / Bing / 百度 / Brave / wigolo）同样遵循上述 MCP Server 封装规范，每个搜索源实现为独立的 MCP Server，由 ConfigAgent 动态注册到 SearchDispatcher。与学术源的区别在于：学术源在系统启动时预注册，通用源在用户提供 API Key 后按需注册。

**通用搜索源 MCP Server 清单**：

| Server | 功能 | 核心工具 | 认证方式 | 免费额度 | 优先级 |
|--------|------|----------|----------|----------|--------|
| **tavily-server** | AI 优化网页搜索 | `web_search`、`search_with_answer` | API Key | 1000 次/月 | 🔴 高 |
| **bing-server** | 通用网页搜索 | `web_search`、`news_search` | API Key | 1000 次/月 | 🟡 中 |
| **baidu-server** | 中文网页搜索 | `web_search`、`news_search` | API Key | 1500 次/月 | 🟡 中 |
| **brave-server** | 隐私网页搜索 | `web_search`、`suggest` | API Key | 2000 次/月 | 🟡 中 |
| **wigolo-server** | 本地免费搜索 | `web_search` | 无需配置 | 无限 | 🟢 低 |

### 每个 MCP Server 标准组件

```
backend/app/skills/{server_name}/
├── __init__.py          # Server 注册入口
├── server.py            # MCP Server 定义（@mcp.tool 装饰器）
├── config.py            # 配置（API base、认证、限流）
├── service.py           # 业务逻辑（HTTP 客户端、数据转换）
└── tests/
    ├── __init__.py
    └── test_{server}.py # 单元测试
```

### MCP Manager 核心设计

```python
class MCPManager:
    """统一调度所有 MCP Server"""

    def __init__(self):
        self.servers: Dict[str, MCPServer] = {}
        self.health_checker = ServiceHealthChecker()

    async def register_server(self, name: str, server: MCPServer):
        """注册 MCP Server（由 ConfigAgent 调用）"""
        self.servers[name] = server

    async def call_tool(self, server_name: str, tool_name: str, params: dict) -> Any:
        """Agent 通过 MCP Manager 调用工具"""
        server = self.servers.get(server_name)
        if not server:
            raise MCPError(f"Server {server_name} 未注册", err_code="ERR-4003")

        try:
            result = await asyncio.wait_for(
                server.call_tool(tool_name, params),
                timeout=2.0  # 2s 超时，自动 fallback
            )
            return result
        except asyncio.TimeoutError:
            # 超时降级：返回缓存结果或提示用户
            raise MCPError(f"Server {server_name} 响应超时", err_code="ERR-4002")
        except MCPOfflineError:
            # 离线降级：跳过 MCP 调用，使用本地数据
            return await self._offline_fallback(server_name, tool_name, params)
```

### ConfigAgent 与 MCP 的集成

```
用户: “帮我连接 arXiv”
  │
  ▼
ConfigAgent:
  1. 识别服务类型 → arXiv
  2. 验证连接 → 发送测试请求
  3. 配置成功 → 调用 MCPManager.register_server("arxiv", ArxivServer())
  4. 通知用户 → “arXiv 已连接，你现在可以说'搜索关于 X 的论文'”
  │
  ▼
用户: “搜索关于 transformer 的论文”
  │
  ▼
Agent:
  1. 意图识别 → 需要外部论文搜索
  2. 工具选择 → MCPManager.call_tool("arxiv", "search_papers", {query: "transformer"})
  3. 结果返回 → 格式化展示给用户
```

### 性能影响与降级策略

| 场景 | 策略 | 延迟影响 |
|------|------|----------|
| MCP Server 正常响应 | 直接调用，返回结果 | 增加 ~200-500ms（网络延迟） |
| MCP Server 超时（>2s） | 自动 fallback 到本地缓存或提示用户 | 最多等待 2s |
| MCP Server 离线 | 跳过 MCP 调用，使用本地 RAG 数据 | 0ms（无额外开销） |
| ConfigAgent 配置新 Server | 注册到 MCP Manager，不重启服务 | 一次性 ~1s |

**待办事项**：

- [ ] 搭建 MCP Manager 基础框架（注册/路由/超时/降级）
- [ ] 实现 arxiv-server（搜索 + PDF 下载 + 元数据）
- [ ] 实现 semantic-scholar-server（语义搜索 + 引用网络）
- [ ] 实现 crossref-server（DOI 解析）
- [ ] 实现 dblp-server（CS 文献检索）
- [ ] 实现 zotero-server（文献库同步）
- [ ] 实现 report-generator-server（结构化报告）
- [ ] ConfigAgent 适配 MCP 注册协议
- [ ] 每个 Server 配套单元测试

---

## 五、Phase 5 — 多模态深度理解（7-9 月）

> 目标：从纯文本理解升级为多模态深度理解，让 PaperChat 能理解论文中的图表、公式、表格等非文本内容。
>
> **参赛节奏说明**：初赛预计 9 月，Phase 5 的核心多模态功能（图表解析、公式识别、多模态搜索）需在 9 月前完成，领域微调模型可作为进阶展示项。

### 5.1 全研究生命周期覆盖

当前只覆盖“读”和“问”，远期应覆盖完整研究流程：

| 阶段 | 当前能力 | 远期目标 |
|------|----------|----------|
| **发现** | 关键词搜索、相似推荐 | 研究趋势追踪、自动推送前沿、个性化推荐引擎 |
| **阅读** | PDF 阅读、高亮标注 | 多模态理解（图表/公式/代码）、结构化信息提取 |
| **理解** | RAG 问答、术语解释 | 论文方法论对比、实验复现指引、批判性分析 |
| **整理** | 笔记、知识卡片 | 自动知识图谱构建、文献矩阵、研究脉络可视化 |
| **写作** | 基础写作辅助 | 文献综述生成、引用管理、格式适配（LaTeX/Word） |
| **投稿** | 无 | 期刊匹配推荐、审稿意见预判、修改建议 |

---

### 5.2 多模态深度理解

当前仅处理文本，学术论文大量信息在图表中：

| 能力 | 说明 | 技术路径 |
|------|------|----------|
| **图表解析** | 理解论文中的折线图、柱状图、散点图 | 多模态大模型（Qwen-VL 端侧部署） |
| **公式识别** | LaTeX 公式提取与语义理解 | Nougat 模型（端侧部署） |
| **表格结构化** | 表格数据提取为可计算格式 | Table Transformer |
| **代码提取** | 论文中代码片段提取与可运行化 | AST 解析 + 沙箱执行 |

**与 Chat 的关系**：多模态理解作为 Chat 的新工具链，用户说"解释一下图 3 的趋势"时自动路由到图表解析工具。

**多模态搜索场景**：

将图表解析结果与搜索结合，形成"图表 → 搜索 → 相关论文"的闭环：

```
用户: [拖拽一张论文图表到 Chat 输入框]

Agent 多模态搜索流程:
  │
  ├── 1. 图表解析 → 提取关键实体（标题、坐标轴标签、数据趋势）
  ├── 2. 自动生成搜索查询（从图表标题和坐标轴标签生成关键词）
  ├── 3. 调用 Phase 4 搜索集群 → 返回相关论文
  └── 4. 展示结果："根据图表中的'Attention Mechanism'和'F1 Score'关键词，找到以下相关论文..."
```

**待办事项**：

- [ ] 图表解析服务（Qwen-VL 端侧部署）
- [ ] 公式识别与 LaTeX 提取（Nougat 端侧）
- [ ] 表格结构化提取（Table Transformer）
- [ ] PDFReader 支持图表/公式/表格交互
- [ ] Chat 新增多模态意图路由
- [ ] 实现从图表到搜索查询的自动转换（例如从图表标题和坐标轴标签生成关键词）
- [ ] 前端支持拖拽图片到 Chat 输入框

---

### 5.3 领域微调模型 + 评估体系

| 阶段 | 技术方案 | 目标 |
|------|----------|------|
| **领域微调** | LoRA/QLoRA 微调 Qwen3-35B-A3B | 学术问答准确率提升 20%+ |
| **评估体系** | RAGAS 框架 + 自动化评测 | 量化模型效果，持续迭代 |

**待办事项**：

- [ ] 领域微调数据集构建
- [ ] LoRA/QLoRA 微调流水线
- [ ] RAGAS 评估框架搭建
- [ ] 端侧微调模型部署

---

## 六、Phase 6 — AMD 锐龙 AI MAX+ 端侧适配 + 本地推理（9-10 月）

> 目标：将 PaperChat 从云端 API 依赖转向 AMD 锐龙 AI MAX+ 端侧推理，实现隐私保护、离线可用、低延迟的端侧 AI 智能体。
>
> 这是参赛的核心技术基础——赛题要求“使用 AMD 指定的 APU 产品开发一个 AI 应用”。功能开发完成后统一适配，确保端到端展示效果。

### 6.1 AMD 锐龙 AI MAX+ 平台部署

**平台能力**：

| 能力 | 规格 | PaperChat 用途 |
|------|------|----------------|
| CPU | 16 核 32 线程 Zen5 | FastAPI 后端 + ChromaDB 向量检索 |
| NPU | 50 TOPS (XDNA 2) | 意图识别、文本分类等轻量推理 |
| iGPU | 40 CU Radeon 8060S | 大模型推理（Qwen3-35B-A3B / Gemma-31B） |
| 统一内存 | 最高 128GB，96GB 可分配显存 | 运行大模型 + RAG 向量库 |
| 内存带宽 | 256 GB/s 四通道 | 多 Agent 并发推理 |

**部署方案**：

```
┌─────────────────────────────────────────────────────┐
│              AMD 锐龙 AI MAX+ 395                    │
│                                                      │
│  ┌──────────┐  ┌──────────┐  ┌───────────────────┐  │
│  │  NPU     │  │  iGPU    │  │  CPU              │  │
│  │ 50 TOPS  │  │ 40 CU    │  │ 16C/32T Zen5     │  │
│  │          │  │ 96GB VRAM│  │                   │  │
│  │ 意图识别  │  │ 35B MoE  │  │ FastAPI + ChromaDB│  │
│  │ 文本分类  │  │ 模型推理  │  │ Agent Orchestrator│  │
│  │ 实体提取  │  │ RAG Rerank│  │ WebSocket Server  │  │
│  └──────────┘  └──────────┘  └───────────────────┘  │
│                                                      │
│  ┌──────────────────────────────────────────────┐    │
│  │  128GB 统一内存                               │    │
│  │  ├─ 模型权重 (~20GB for Qwen3-35B-A3B Q4)   │    │
│  │  ├─ RAG 向量索引 (~2-4GB)                    │    │
│  │  ├─ 知识图谱缓存 (~1GB)                      │    │
│  │  └─ 系统及其他 (~剩余)                        │    │
│  └──────────────────────────────────────────────┘    │
└─────────────────────────────────────────────────────┘
```

**待办事项**：

- [ ] 搭建 AMD 锐龙 AI MAX+ 开发环境
- [ ] 安装 ROCm / ONNX Runtime DirectML 驱动
- [ ] 验证 NPU / iGPU / CPU 三级推理可用性
- [ ] 性能基准测试（各硬件单元的推理吞吐量）

---

### 6.2 推理后端集成

**多后端架构**：

```
用户请求
  │
  ▼
Model Router（模型路由层）
  │
  ├── 轻量任务（意图识别/分类）──→ NPU (ONNX DirectML)
  │   └── 延迟 < 50ms，功耗极低
  │
  ├── 中等任务（问答/摘要/翻译）──→ iGPU (llama.cpp / ROCm)
  │   └── Qwen3-35B-A3B (3B 激活)，30-50 tokens/s
  │
  ├── 重度任务（深度分析/综述）──→ iGPU (llama.cpp / ROCm)
  │   └── Gemma-31B / Qwen3-35B-A3B 全量，15-30 tokens/s
  │
  └── 云端备用（高并发/大模型）──→ DeepSeek API
      └── 仅在端侧不可用时降级
```

| 后端 | 框架 | 适用场景 | 模型 |
|------|------|----------|------|
| **ONNX DirectML** | onnxruntime-directml | NPU 推理（意图识别、分类） | Qwen3-0.6B/1.7B ONNX |
| **llama.cpp + ROCm** | llama.cpp + ROCm/Vulkan | iGPU 推理（问答、分析、深度推理） | Qwen3-35B-A3B / Gemma-31B GGUF |
| **DeepSeek API** | httpx | 云端降级 | DeepSeek-V3 |

**待办事项**：

- [ ] ONNX DirectML 推理管道（NPU 意图识别）
- [ ] llama.cpp + ROCm 部署 Qwen3-35B-A3B / Gemma-31B
- [ ] llama.cpp + Vulkan 备用推理链路（ROCm 不可用时降级）
- [ ] Model Router 统一接口（自动选择后端）
- [ ] 推理性能基准测试与调优

---

### 6.3 智能路由与成本控制引擎

> 在原有按复杂度和隐私路由的基础上，深度融合用户主动控制和成本约束，让路由决策更符合用户预期。

**路由策略（增强版）**：

```python
class ModelRouter:
    async def route(self, task: Task, user_prefs: UserPreferences) -> InferenceBackend:
        # 1. 用户全局模式覆盖
        if user_prefs.model_mode == "local_only":
            return self.local_backend
        if user_prefs.model_mode == "cloud_only":
            return await self._check_cloud_budget(task, user_prefs)

        # 2. 智能路由模式：先判断复杂度
        recommended = self._recommend_by_complexity(task)

        # 3. 成本控制：检查月度预算和单次确认阈值
        if recommended.is_cloud:
            if not self._within_budget(user_prefs):
                return self.local_backend  # 超预算降级
            if self._need_confirm(task, user_prefs):
                # 触发 Phase 3.4 的费用确认弹窗
                if not await self._ask_user(task, recommended):
                    return self.local_backend

        # 4. 隐私路由
        if task.privacy_level == "strict":
            return self.local_backend

        # 5. 免费额度感知（Tavily/Bing/百度等额度作为路由决策因素）
        if recommended.is_cloud and not self._has_free_quota(recommended):
            alternative = self._find_free_alternative(task)
            if alternative:
                return alternative

        return recommended
```

**隐私路由**：

| 场景 | 路由策略 | 说明 |
|------|----------|------|
| 普通论文问答 | 端侧优先，云端降级 | 优先使用本地模型 |
| 隐私论文（用户标记） | **强制端侧** | 数据绝不上传云端 |
| 离线模式 | 仅端侧 | 无网络时仍可使用 |
| 高并发场景 | 端侧 + 云端混合 | 端侧处理简单任务，云端处理复杂任务 |
| 超预算 | 自动降级端侧 | 月度费用超限后自动切换本地 |
| 单次超阈值 | 弹窗确认 | 预估费用超阈值时询问用户 |

**成本控制机制**：

| 机制 | 说明 |
|------|------|
| 月度预算检查 | 每次云端调用前检查本月累计费用，超限自动降级本地 |
| 单次确认阈值 | 预估费用超过用户设定阈值时，弹窗确认（复用 Phase 3.4 的费用确认弹窗组件） |
| 免费额度追踪 | 追踪 Tavily/Bing/百度等搜索源的免费额度剩余，作为路由决策因素 |
| 费用实时推送 | 每次推理后通过 WebSocket 推送费用信息到前端（复用 Phase 3.4 的实时模型指示器） |
| 预算仪表盘联动 | 月度费用数据同步到 Phase 3.4 的预算仪表盘展示 |

**待办事项**：

- [ ] ModelRouter 类实现（任务分类 + 后端选择）
- [ ] 用户偏好读取集成到 ModelRouter
- [ ] 月度预算检查与自动降级
- [ ] 单次确认阈值触发弹窗（复用 Phase 3.4 的组件）
- [ ] 免费额度追踪（Tavily/Bing/百度等）作为路由决策的一个因素
- [ ] 隐私论文标记与强制路由
- [ ] 离线模式检测与自动切换
- [ ] 端侧/云端混合推理策略
- [ ] 路由状态前端展示（当前使用哪个后端）

---

### 6.4 RAG 增强（端侧优化）

| 增强 | 说明 | 端侧优势 |
|------|------|----------|
| **Reranker** | bge-reranker-v2-m3 重排序 | 本地运行，无需上传查询 |
| **HyDE** | 假设文档增强检索 | 本地 LLM 生成假设文档 |
| **混合检索** | BM25 + 向量检索融合 | BM25 缓存 + ChromaDB 本地 |
| **长上下文** | 利用 96GB 显存支持长文档 | 端侧可加载更大上下文窗口 |

**待办事项**：

- [ ] bge-reranker-v2-m3 ONNX 端侧部署
- [ ] HyDE 本地生成管道
- [ ] BM25 + 向量混合检索实现
- [ ] RAG 端到端性能测试

---

### 6.5 端侧多 Agent 并发推理 + ConfigAgent 协同

**赛题亮点**：展示多个 Agent 在端侧同时运行的能力，以及 ConfigAgent + 端侧推理的组合。

```
AMD 锐龙 AI MAX+ 96GB 统一内存分配：
├── Orchestrator Agent ──── Qwen3-35B-A3B (共享模型权重)
├── Retriever Agent   ──── RAG + BM25 (共享向量库)
├── Analyzer Agent    ──── Qwen3-35B-A3B (共享模型权重)
├── Recommender Agent ──── Qwen3-35B-A3B (共享模型权重)
└── ConfigAgent       ──── Qwen3-35B-A3B (共享模型权重)

内存占用估算：
├── 模型权重 (Qwen3-35B-A3B Q4_K_M): ~20GB × 1（共享）
├── KV Cache (5 Agent 并发): ~3GB
├── ChromaDB 向量索引: ~2-4GB
├── 系统占用: ~4GB
└── 剩余可用: ~65GB+（充裕）
```

**关键优化**：

| 优化 | 说明 |
|------|------|
| 模型权重共享 | 多 Agent 共享同一份模型权重，避免重复加载 |
| 连续批处理 | 多 Agent 请求合并为 batch，提高 GPU 利用率 |
| KV Cache 复用 | 相同上下文的 Agent 共享 KV Cache |
| 请求优先级 | 用户交互请求优先，后台分析请求低优先级 |

**ConfigAgent 端侧协同场景**：

```
场景: 用户在离线环境下阅读论文

用户: "这篇论文引用的 Smith 2023 的论文讲了什么？"

Agent 推理:
  1. 检查本地论文库 → 未找到 Smith 2023
  2. 检查外部服务 → 离线，无法访问
  3. 端侧 RAG 检索 → 找到当前论文中对 Smith 2023 的引用描述
  4. 基于引用上下文生成回答:
     "我目前处于离线状态，无法下载 Smith 2023 的原文。
      但根据当前论文的引用描述，该论文主要提出了..."
  5. 记录待办: "网络恢复后自动下载 Smith 2023"

  → 展示端侧智能体的自主决策能力
```

**ConfigAgent 离线协同场景 2**：

```
场景: 用户在离线状态下说"帮我连接 Zotero"

用户: "帮我连接 Zotero"

ConfigAgent 推理:
  1. 检测到当前离线，无法验证 API Key
  2. 回复: "当前离线，无法验证 API Key。我已将你的请求加入离线队列，
     网络恢复后将自动尝试连接并通知你。"
  3. 将配置请求写入离线队列（OfflineTask）
  4. 网络恢复后 → 自动重试 → 成功连接 → 通知用户

  → 展示端侧智能体的自主排队能力
```

**待办事项**：

- [ ] 多 Agent 共享模型权重机制
- [ ] 连续批处理调度器
- [ ] KV Cache 复用策略
- [ ] 并发性能基准测试（1/2/4/5 Agent 并发）
- [ ] 端侧 vs 云端性能对比报告
- [ ] 离线模式下 ConfigAgent 降级策略
- [ ] 网络恢复后自动执行待办任务
- [ ] 端侧 + 外部服务混合检索策略
- [ ] 赛题 Demo 场景脚本编写
- [ ] ConfigAgent 检测网络状态并切换为"离线配置预览模式"
- [ ] 离线配置请求入队，网络恢复后自动重试

---

### 6.6 离线优先与主动预缓存

> 突出端侧智能体的主动性——离线时仍能提供有价值的服务，在线时主动预缓存用户可能需要的内容。

| 功能 | 说明 | 技术实现 |
|------|------|----------|
| **离线队列** | 用户离线时发出的联网命令自动入队，网络恢复后按序执行 | SQLite 本地队列 + 后台轮询 |
| **预缓存策略** | 用户可标记感兴趣领域，后台预缓存 arXiv 前 200 篇热门论文元数据 | 定时任务 + 本地向量库 |
| **离线搜索** | 即使无网络，也能基于预缓存元数据进行模糊搜索 | 本地 BM25 + 向量检索 |

**离线队列数据模型**：

```python
class OfflineTask(Base):
    user_id = Column(Integer, ForeignKey("users.id"))
    task_type = Column(String)       # "search" | "download" | "sync" | "config"
    task_params = Column(JSON)       # 任务参数
    status = Column(String)          # "pending" | "running" | "done" | "failed"
    created_at = Column(DateTime)
    executed_at = Column(DateTime, nullable=True)
    result = Column(JSON, nullable=True)
```

**预缓存订阅**：

```
用户: "每周预缓存计算机视觉最新论文"

Agent:
  1. 创建预缓存订阅（领域=CV，频率=每周，数量=200）
  2. 定时任务：每周调用 arXiv API 获取最新论文元数据
  3. 存入本地向量库 + BM25 索引
  4. 用户离线时仍可搜索这些预缓存的论文
```

**待办事项**：

- [ ] 离线任务队列存储与调度
- [ ] 用户可配置的预缓存订阅（如"每周预缓存计算机视觉最新论文"）
- [ ] 离线状态检测与 UI 提示
- [ ] 离线搜索（基于预缓存元数据的 BM25 + 向量检索）

---

## 七、Phase 7 — 团队协作平台（11-12 月）

> 目标：从个人工具升级为团队平台，支持研究组协作。

### 7.1 组织与项目管理

| 功能 | 说明 |
|------|------|
| **研究组** | 创建研究组，按项目组织论文和笔记 |
| **项目空间** | 每个项目独立论文库、知识库、讨论区 |
| **成员管理** | 邀请成员加入研究组，分配项目 |

**数据模型变更**：

```python
class Organization(Base):
    name = Column(String)
    owner_id = Column(Integer, ForeignKey("users.id"))

class Project(Base):
    org_id = Column(Integer, ForeignKey("organizations.id"))
    name = Column(String)
    description = Column(Text)

class ProjectMember(Base):
    project_id = Column(Integer, ForeignKey("projects.id"))
    user_id = Column(Integer, ForeignKey("users.id"))
    role = Column(String)  # "admin" | "member" | "viewer"
```

---

### 7.2 共享知识库 + 协同标注

| 功能 | 说明 |
|------|------|
| **共享论文库** | 团队论文库共享，标注和笔记同步 |
| **协同阅读** | 多人同时标注同一论文，评论与讨论 |
| **标注同步** | 实时 WebSocket 推送他人标注 |

**技术方案**：WebSocket 广播标注变更，CRDT 解决冲突

---

### 7.3 任务分配 + 研究看板

| 功能 | 说明 |
|------|------|
| **任务分配** | 导师分配阅读任务，学生提交阅读报告 |
| **研究看板** | 团队研究进度可视化（类似 Trello） |
| **进度追踪** | 论文阅读状态、分析完成度、写作进度 |

---

### 7.4 权限管理

| 角色 | 论文 | 标注/笔记 | 知识库 | 管理操作 |
|------|------|-----------|--------|----------|
| **管理员** | 全部 | 全部 | 全部 | 成员管理、项目设置 |
| **成员** | 全部 | 自己+可见 | 全部 | — |
| **访客** | 可见 | 只读 | 只读 | — |

**待办事项**：

- [ ] Organization / Project / ProjectMember 数据模型
- [ ] RBAC 权限中间件
- [ ] 共享论文库 API
- [ ] 协同标注 WebSocket 广播
- [ ] 研究看板 UI
- [ ] 任务分配与进度追踪

---

## 八、Phase 8 — 开放生态（次年 Q1）

> 目标：从封闭产品升级为开放平台，构建学术研究基础设施。

### 8.1 插件系统

| 层级 | 说明 |
|------|------|
| **工具插件** | 新增 Agent 工具（如新论文源、新分析工具） |
| **模型插件** | 接入新模型（如 Claude、Gemini） |
| **数据源插件** | 新增论文来源（ConfigAgent 已支持动态注册） |
| **渲染插件** | 新增内容渲染器（如 LaTeX 预览、代码运行） |

**与 ConfigAgent 的关系**：Phase 5 的 ConfigAgent 已实现"动态注册工具"的核心能力，插件系统是 ConfigAgent 的标准化扩展。

---

### 8.2 开放 API

提供 REST API 供外部系统调用：

| API | 说明 |
|-----|------|
| `GET /api/v1/papers` | 论文列表 |
| `POST /api/v1/chat` | 问答接口 |
| `GET /api/v1/knowledge` | 知识库查询 |
| `POST /api/v1/analyze` | 论文分析 |
| `GET /api/v1/recommendations` | 推荐列表 |
| `GET /api/v1/services` | 已配置外部服务列表 |
| `POST /api/v1/services/config` | 配置外部服务 |

---

### 8.3 模板市场 + Agent 市场

| 市场 | 说明 |
|------|------|
| **论文阅读模板** | 不同学科（CS/医学/社科）的阅读模板 |
| **写作模板** | 综述/开题/论文各章节模板 |
| **分析模板** | 实验对比/方法论分析/批判性阅读模板 |
| **Agent 流程** | 用户自建 Agent 工作流并分享 |
| **服务连接模板** | ConfigAgent 的服务配置模板分享 |

---

### 8.4 Tauri 桌面端

| 项目 | 说明 |
|------|------|
| 框架 | Tauri 2.x |
| 体积 | 5-10MB（vs Electron 150MB+） |
| 核心优势 | 前端代码零改动，React + Vite 直接嵌入 |

**桌面端特有功能**：

| 功能 | 实现方式 |
|------|----------|
| 本地 PDF 直接打开 | `tauri-plugin-fs` |
| 系统托盘 | `tauri-plugin-tray` |
| 原生通知 | `tauri-plugin-notification` |
| 全局快捷键 | `tauri-plugin-global-shortcut` |
| 自动更新 | `tauri-plugin-updater` |

**待办事项**：

- [ ] 插件注册与加载机制
- [ ] 开放 API 文档（OpenAPI/Swagger）
- [ ] 模板市场 UI + 后端
- [ ] Agent 工作流编辑器
- [ ] Tauri 桌面端初始化与打包

---

### 商业化方向（参考）

| 模式 | 说明 |
|------|------|
| **开源核心** | 基础功能开源，吸引社区 |
| **Pro 订阅** | 高级 Agent、多模态、团队协作 |
| **私有部署** | 企业/高校私有化部署，数据不出域 |
| **API 计费** | 开放 API 按调用收费 |
| **端侧一体机** | 预装 AMD 锐龙 AI MAX+ 的 PaperChat 研究工作站 |

---

## 九、关于 Agent 的使用原则

> 不是所有功能都需要 Agent。以下原则指导何时使用 Agent。

### 适合用 Agent 的场景

- **需要 LLM 自主决策下一步**：用户意图不明确，需要推理选择工具
- **步骤不确定**：不同输入可能走不同路径，无法硬编码流程
- **需要多步推理**：前一步的输出决定后一步的输入
- **需要动态扩展能力**：ConfigAgent 根据用户提供的 API Key 自主配置新工具

### 不适合用 Agent 的场景

- **确定性流水线**：输入 → 处理 → 输出，步骤固定（如知识图谱构建）
- **纯算法问题**：推荐、排序、分类（如论文推荐）
- **单次 LLM 调用**：翻译、摘要、润色（如写作辅助的大部分功能）
- **批量重复任务**：对 N 篇论文做相同操作（如批量分析）

### 当前项目中的判断

| 功能 | 是否需要 Agent | 原因 |
|------|----------------|------|
| 问答（意图识别 + 工具调用） | ✅ 单 Agent | 意图不确定，需动态选择工具 |
| 研究助手（检索→分析→推荐） | ✅ 多 Agent | 多步骤协作，需并行+整合 |
| 复杂写作（文献综述） | ✅ 复用 Agent | 需规划步骤，但复用已有框架 |
| **ConfigAgent（自主配置服务）** | **✅ 单 Agent** | **步骤不确定，需诊断错误并自主修复** |
| 阅读辅助（选词→解释） | ❌ | 确定性映射，直接调用工具 |
| 知识图谱构建 | ❌ | 确定性流水线 |
| 论文推荐 | ❌ | 算法问题 |
| 批量分析 | ❌ | 重复任务，异步队列即可 |
| 翻译/摘要/润色 | ❌ | 单次 LLM 调用 |

---

## 九-B、工程规范化改进

> 目标：系统性地提升工程规范性，消除技术债务，为长期选代打下坚实基础。
> 对应 Phase 3.5 的详细实施方案，此处为集中索引。

### P0 级 — 必须立即实施

| 序号 | 规范项 | 当前问题 | 目标状态 | 性能影响 |
|------|--------|----------|----------|----------|
| E1 | 提示词版本管理 | 提示词分离已完成，但无版本追踪 | 每个提示词文件有版本号和变更日志 | 无 |
| E2 | 结构化错误码 | 错误信息为自由文本字符串 | ERR-XXXX 编码体系，前端可差异化处理 | 可忽略 |
| E3 | 工具权限分级 | 工具无权限控制 | public/authenticated/admin 三级 | 可忽略 |
| E3.5 | 隐私与安全规范 | 缺少整体隐私政策 | 数据最小化声明 + API Key 审计 + 匿名模式 | 可忽略 |

### P1 级 — 尽快实施

| 序号 | 规范项 | 当前问题 | 目标状态 | 性能影响 |
|------|--------|----------|----------|----------|
| E4 | Prettier + Pre-commit | 代码风格不统一，无自动格式化 | 提交时自动格式化 | 提交增加 ~2-5s |
| E5 | TypeScript strict | strict 未启用，隐式 any 泛滥 | strict: true 全量启用 | 编译增加 ~10-20% |
| E6 | 核心路径测试覆盖 | 仅 2 个测试文件，覆盖率极低 | 核心路径 ≥ 60% | 测试增加 ~30s |
| E7 | MCP Server 标准化 | 外部服务集成无统一规范 | 6 个 Server 统一目录和协议 | MCP 延迟 ~200-500ms |

### 实施路线图

```
Phase 3.5 第 1 周:
  ├── E1 提示词版本管理（1 天）
  ├── E2 结构化错误码（2 天）
  └── E3 工具权限分级（1 天）

Phase 3.5 第 2 周:
  ├── E4 Prettier + Pre-commit（0.5 天）
  ├── E5 TypeScript strict 迁移（2 天，渐进式）
  ├── E6 核心路径测试覆盖（2 天）
  └── E7 MCP Server 基础框架（2 天）

Phase 3.5 第 3 周（新增）:
  ├── E3.5 隐私与安全规范（1 天）
  ├── E8 多级配置体系（2 天）
  └── E9 上下文压缩机制（2 天）
```

---

## 九-C、Phase 9 — 基础设施建设具体任务

> Phase 3.5 已完成的基础规范（Prettier、TS strict、测试、错误码、权限分级、隐私安全）不再重复列出，本阶段聚焦于运维、安全、性能等长期基建。

### 9.1 具体基建任务

| 序号 | 基建项 | 说明 | 建议优先级 | 建议时间 |
|------|--------|------|------------|----------|
| I1 | 结构化日志 | 统一日志格式（JSON），支持按模块/级别过滤，替代 print | P1 | Phase 3 期间 |
| I1-B | 启动 Profiling | 记录各服务初始化耗时（DB/LLM/RAG/Agent 等），辅助启动优化决策 | P2 | Phase 3 期间 |
| I1-C | API 调用成本追踪 | 记录每次 LLM/搜索调用的 Token 消耗、费用、延迟，支持用量分析 | P1 | Phase 4 期间 |
| I1-D | UI 渲染性能监控 | 前端 FPS 监控、组件重渲染追踪、打字机效果流畅度指标 | P3 | Phase 3 期间 |
| I2 | API 版本化 | URL 前缀 `/api/v1/`，为后续开放 API 做准备 | P1 | Phase 4 期间
| I3 | CI/CD | GitHub Actions：lint + test + build 自动化 | P1 | Phase 3.5 期间 |
| I4 | 请求限流 | FastAPI 中间件，按 IP/User 限流 | P2 | Phase 4 期间 |
| I5 | 错误边界 | 前端 React Error Boundary，防止组件崩溃影响全局 | P2 | Phase 3 期间 |
| I6 | 国际化 | i18n 框架（react-i18next），先支持中英文 | P3 | Phase 7 期间 |
| I7 | 连接池调优 | 数据库连接池大小、超时、回收策略 | P2 | Phase 6 期间 |
| I8 | 优雅关停 | FastAPI shutdown 事件处理，确保请求完成 | P2 | Phase 6 期间 |
| I9 | 数据备份 | 定时备份数据库和向量库 | P2 | Phase 6 期间 |
| I10 | Feature Flag | 功能开关框架，支持灰度发布 | P3 | Phase 7 期间 |
| I11 | 事件驱动架构 | 引入事件总线，解耦模块间同步调用 | P3 | Phase 7 期间 |
| I12 | 安全加固 | HTTPS 强制、CORS 策略、XSS/CSRF 防护 | P1 | Phase 4 期间 |
| I13 | 数据库索引优化 | 慢查询分析 + 索引优化 | P2 | Phase 6 期间 |
| I14 | 消除硬编码 user_id | 从 JWT token 获取，支持多用户 | P0 | Phase 1 期间 |

**待办事项**：

- [ ] I1 结构化日志框架搭建
- [ ] I1-B 启动 Profiling 记录与可视化
- [ ] I1-C API 调用成本追踪服务（Token/费用/延迟统计）
- [ ] I1-D UI 渲染性能监控（FPS/重渲染/打字机流畅度）
- [ ] I2 API 版本化路由前缀
- [ ] I3 CI/CD 流水线（lint + test + build）
- [ ] I4 请求限流中间件
- [ ] I5 前端 Error Boundary
- [ ] I6 国际化框架搭建
- [ ] I7 数据库连接池调优
- [ ] I8 优雅关停处理
- [ ] I9 数据备份定时任务
- [ ] I10 Feature Flag 框架
- [ ] I11 事件驱动架构引入
- [ ] I12 安全加固（HTTPS/CORS/XSS/CSRF）
- [ ] I13 数据库索引优化
- [ ] I14 消除硬编码 user_id

---

## 十、开发优先级总表

### 近期（Phase 1-3.5）

| 功能 | 类型 | 优先级 | 需要 Agent | 复杂度 |
|------|------|--------|------------|--------|
| Agent 意图识别上线 | Chat 统一入口 | **P0** | ✅ 单 | 低 |
| 写作功能收拢到 Chat | Chat 统一入口 | **P0** | 部分 | 中 |
| 阅读辅助面板 | Chat 统一入口 | **P0** | ❌ | 低 |
| 上下文压缩机制 | 关键优化 | **P0** | ❌ | 中 |
| 知识库/论文查询收拢到 Chat | Chat 统一入口 | **P1** | ❌ | 低 |
| 多 Agent 研究助手 | 智能化 | **P1** | ✅ 多 | 极高 |
| 简报生成 Agent（Briefing Mode） | 智能化 | **P1** | ✅ 单 | 高 |
| 知识图谱构建 | 智能化 | **P2** | ❌ | 高 |
| Pretext 动效 | 体验 | **P2** | ❌ | 低 |
| 推荐系统增强 | 体验 | **P2** | ❌ | 中 |
| 批量论文分析 | 体验 | **P3** | ❌ | 中 |
| 用户控制与成本感知面板 | 体验 | **P1** | ❌ | 中 |
| 内嵌 WebView 组件 | 体验 | **P2** | ❌ | 中 |
| 启动性能优化 | 性能 | **P2** | ❌ | 中 |
| 提示词版本管理 | 工程规范 | **P0** | ❌ | 低 |
| 结构化错误码体系 | 工程规范 | **P0** | ❌ | 中 |
| 工具权限分级控制 | 工程规范 | **P0** | ❌ | 低 |
| 多级配置体系 | 工程规范 | **P1** | ❌ | 中 |
| 隐私与安全规范 | 工程规范 | **P1** | ❌ | 中 |
| API 调用成本追踪 | 可观测性 | **P1** | ❌ | 中 |
| MCP Manager 基础框架 | MCP 基建 | **P1** | ❌ | 中 |

### 赛题核心（Phase 4-6）★

| 功能 | 类型 | 优先级 | Phase | 需要 Agent | 复杂度 |
|------|------|--------|-------|------------|--------|
| ConfigAgent 自主配置 + 配置预览与安全确认 | 智能化 | **P0** | 4 | ✅ 单 | 高 |
| ConfigAgent 适配 MCP 协议 | 智能化 | **P0** | 4 | ✅ 单 | 中 |
| 学术 MCP Server 工具库上线 | MCP 基建 | **P0** | 4 | ❌ | 高 |
| 多源搜索服务集群（学术源 + 通用搜索 + SearchDispatcher） | 外部服务 | **P0** | 4 | ❌ | 中 |
| 引用管理集成（Zotero / Mendeley）— MCP 驱动 | 外部服务 | **P1** | 4 | ❌ | 中 |
| 结构化报告生成 Server | MCP 基建 | **P1** | 4 | ❌ | 中 |
| API Key 安全管理 | 安全 | **P1** | 4 | ❌ | 中 |
| 服务健康检测 | 运维 | **P2** | 4 | ❌ | 低 |
| ConfigAgent 功能验证与集成测试 | 测试 | **P1** | 4 | ❌ | 中 |
| 图表解析 | 多模态 | **P1** | 5 | ❌ | 高 |
| 公式识别 | 多模态 | **P2** | 5 | ❌ | 高 |
| 表格结构化 | 多模态 | **P2** | 5 | ❌ | 中 |
| 多模态搜索（图表 → 搜索 → 相关论文） | 多模态 | **P1** | 5 | ❌ | 中 |
| 领域微调模型 | 模型 | **P2** | 5 | ❌ | 极高 |
| AMD 锐龙 AI MAX+ 平台部署 | 端侧适配 | **P0** | 6 | ❌ | 高 |
| ONNX DirectML / ROCm 推理后端 | 端侧适配 | **P0** | 6 | ❌ | 高 |
| 智能路由与成本控制引擎 | 端侧适配 | **P0** | 6 | ❌ | 中 |
| 隐私论文端侧强制路由 | 端侧适配 | **P1** | 6 | ❌ | 低 |
| RAG 增强（Reranker + HyDE） | 端侧适配 | **P1** | 6 | ❌ | 中 |
| 离线优先与主动预缓存 | 端侧适配 | **P1** | 6 | ❌ | 中 |
| 端侧多 Agent 并发推理 + ConfigAgent 离线协同 | 端侧适配 | **P1** | 6 | ✅ 多 | 极高 |

### 远期（Phase 7-8）

| 功能 | 类型 | 优先级 | 需要 Agent | 复杂度 |
|------|------|--------|------------|--------|
| 组织与项目管理 | 团队协作 | **P5** | ❌ | 高 |
| 共享知识库 + 协同标注 | 团队协作 | **P5** | ❌ | 高 |
| 权限管理（RBAC） | 团队协作 | **P5** | ❌ | 中 |
| 插件系统 | 开放生态 | **P6** | ❌ | 极高 |
| 开放 API | 开放生态 | **P6** | ❌ | 中 |
| 模板市场 + Agent 市场 | 开放生态 | **P6** | ❌ | 高 |
| Tauri 桌面端 | 平台化 | **P6** | ❌ | 中 |

### 远期里程碑总览

| 时间 | 阶段 | 核心目标 | 关键节点 |
|------|------|----------|----------|
| **1-2 月** | Phase 1-2 | Agent 集成、Chat 统一入口、架构优化 | — |
| **3-4 月** | Phase 2-3 + 3.5 | 多 Agent 研究助手、简报模式、用户控制面板、启动优化、上下文压缩、体验打磨、工程规范化（含多级配置） | — |
| **4 月中** | Phase 3.5 | MCP Manager 基础框架、工程规范化 8 项（含隐私与安全规范） | MCP 工具库基建完成 |
| **5 月** | Phase 4 前半 | ConfigAgent + 配置预览 + arXiv/S2 MCP Server + 多源搜索集群 | 首个外部服务可用 |
| **6 月** | Phase 4 后半 | Zotero MCP Server + 报告生成 Server + 集成测试 | 6 个 MCP Server 全部上线 |
| **6.20** | — | **赛题报名截止** | ★ 报名材料提交 |
| **7-9 月** | Phase 5 | 多模态深度理解（图表/公式/表格 + 多模态搜索 + 领域微调） | 多模态功能上线 |
| **9-10 月** | Phase 6 | AMD 端侧适配、智能路由与成本控制、离线优先与预缓存 | **初赛/决赛提交** |
| **11-12 月** | Phase 7 | 团队协作、共享知识库 | — |
| **次年 Q1** | Phase 8 | 插件系统、开放 API、Tauri 桌面端 | — |

> PaperChat — 从"论文阅读工具"演进为"端侧 AI 智能体研究平台"
