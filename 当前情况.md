# 当前情况 — 代码审计报告与修复方案

> 审计日期：2026-04-24
> 审计范围：ChatPDF Agent 系统代码实现 vs 运行时实际路径

---

## 一、核心结论

ChatPDF 拥有**一套质量不错的 Agent 基础设施**（ReAct 循环、22 个工具、LLM 意图识别、任务规划器），但这些实现**未连接到运行时路径**。关键路由文件 `ws.py` 走的是旧的简单路径，导致大部分 Agent 能力在运行时不可用。

**一句话：代码写完了，线没接上。**

---

## 二、架构断裂全景图

```
前端 ChatPage
  │
  │ 发送 type: 'unified_chat' ✅ 正确
  ▼
ws.py (路由器)
  │
  ├──▶ 旧路径（运行时实际走的）         ◀── 问题在此
  │      ws.py L336-490 内联路由
  │      ├── 只路由到 5 个 handler
  │      ├── 只调用旧版 classify_by_keywords (13 种意图)
  │      ├── 22 个工具中仅 5 个可达
  │      └── ReAct 循环不可达
  │
  └──▶ 新路径（已实现但未接线）         ◀── 死代码
         unified_handler.py ~1000 行
          ├── LLM 三通道意图识别
          ├── 安全检测 + 澄清服务
          ├── 21 种意图 + 22 个工具
          ├── 多步任务规划
          ├── ReAct 循环降级
          └── 用户画像更新
```

---

## 三、详细问题清单

### P0 — 必须立即修复

| # | 问题 | 严重程度 | 影响 |
|---|------|---------|------|
| 1 | `unified_handler.py` ~1000 行完整的 Agent 实现在运行时完全未调用 | **CRITICAL** | 所有高级 Agent 功能不可用 |
| 2 | `ws.py` 路由 `unified_chat` 时自己做内联路由而非调用 `unified_handler` | **CRITICAL** | 架构断裂的根源 |
| 3 | 旧版 `services/agent_service.py` (832 行) 仍在运行时被导入 | **HIGH** | 代码不一致，维护负担 |
| 4 | ReAct 循环 (`react_agent.py` 777 行) 在 `unified_chat` 路径中不可达 | **HIGH** | 核心 Agent 能力浪费 |
| 5 | `unified_chat` 路由仅 5 个 handler，22 个工具中 17 个不可达 | **HIGH** | 功能严重受限 |

### P1 — 尽快修复

| # | 问题 | 严重程度 | 影响 |
|---|------|---------|------|
| 6 | 前端 `ChatPage.jsx` 未渲染 `AgentProgress` 组件 | MEDIUM | Agent 思考过程对用户不可见 |
| 7 | 前端 `useChatMessages.js` 未订阅 agent_* / thinking_* 事件 | MEDIUM | 前端无法处理 Agent 流式消息 |
| 8 | 旧 `services/agent_service.py` vs 新 `services/agent/` 双代码库并行 | MEDIUM | 22 个工具实现了两遍 |
| 9 | `ws.py` 仍导入旧版 `classify_by_keywords` (13 意图) 而非新版 (21 意图) | MEDIUM | 意图识别能力减半 |
| 10 | `ChatPanel.jsx` 有 `sendRagMessage` 降级路径绕过统一路由 | LOW | 偶发走旧路径 |

### P2 — 后续修复

| # | 问题 | 严重程度 | 影响 |
|---|------|---------|------|
| 11 | EventBus 仅用于日志 + 一个后台压缩订阅者，业务解耦未发挥 | LOW | 架构冗余 |
| 12 | AgentProgress 组件有两套实现 (`ChatPage/` 和 `AgentProgress/`) | LOW | 维护负担 |
| 13 | 22 个工具缺少独立测试 | LOW | 回归风险 |
| 14 | `handle_agent_chat` 使用旧的 Plan-then-Execute 而非 ReAct | LOW | 旧代码残留 |

---

## 四、双代码库对照

| 组件 | 旧版（运行时生效） | 位置 | 新版（已实现未接线） | 位置 |
|------|------------------|------|-------------------|------|
| AgentService 主类 | 832 行, 含所有工具 | `services/agent_service.py` | 376 行, 委托到独立模块 | `services/agent/agent_service.py` |
| 意图识别 | 13 种关键词, 内联 | `services/agent_service.py` L377-448 | 21 种, 三通道(关键词+LLM+双通道) | `services/agent/intent.py` |
| 工具 | ~5 个内联工具类 | `services/agent_service.py` L56-370 | 22 个工具, 按分类独立文件 | `services/agent/tools/*.py` |
| 任务规划 | 内联在 agent_service | `services/agent_service.py` L470+ | 独立规划器, 三种策略 | `services/agent/planner.py` |
| ReAct 循环 | 无 | — | 777 行, 完整实现 | `services/agent/react_agent.py` |
| 统一路由 | ws.py 内联 120 行 | `routers/ws.py` L336-490 | ~1000 行完整实现 | `handlers/unified_handler.py` |
| 工具注册表 | ToolRegistry 共享 | `tools/registry.py` | ToolRegistry 共享 | `tools/registry.py` |

---

## 五、修复方案

### 方案 A（推荐）：ws.py 接入 unified_handler

**目标**：让 `ws.py` 在收到 `unified_chat` 消息时调用 `unified_handler.handle_unified_chat()` 而非自己内联路由。

**改动量**：修改 1 个文件，约 20 行代码

```python
# ws.py L336 — 将内联路由替换为这 3 行:

async def handle_unified_chat_message(data, ...):
    from app.handlers.unified_handler import handle_unified_chat
    await handle_unified_chat(data, ...)
```

**验证**：前端 ChatPage 已经发送 `type: 'unified_chat'`，接入后：
- 21 种意图识别立即生效
- 22 个工具全部可达
- ReAct 循环在 deep_research 场景可用
- 安全检测 + 澄清服务上线

**风险**：
- `unified_handler.py` 依赖新版 `services/agent/` 下的模块，需确认所有 import 路径正确
- 旧版 `services/agent_service.py` 的 5 个内联工具与新版的 22 个工具可能存在行为差异
- 需要先灰度测试再全量切换

---

### 方案 B（稳健）：增量替换

**分 3 步走**：

#### Step 1 — 统一路由入口（1 天）

在 `ws.py` 中将 `unified_chat` 的 120 行内联逻辑替换为调用 `unified_handler.py` 的 `handle_unified_chat`。保留旧代码作为注释，方便回滚。

**改动**：`ws.py` L336-490 → 约 20 行

#### Step 2 — 前端接入 Agent 事件（1 天）

| 改动 | 文件 | 内容 |
|------|------|------|
| 订阅 agent_* 事件 | `useChatMessages.js` | 添加 agent_thought/action/observation/step/result/final |
| 渲染 AgentProgress | `ChatPage.jsx` | 导入并渲染 AgentProgress 组件 |
| 移除 sendRagMessage 降级 | `ChatPanel.jsx` | 删除 fallback 路径 |

#### Step 3 — 清理旧代码（0.5 天）

| 清理项 | 说明 |
|--------|------|
| 删除 `services/agent_service.py` | 所有功能已被新模块覆盖 |
| 删除 `agent_handler.py` | 被 unified_handler 替代 |
| 确认 `handlers/` 下旧 handler 是否可归档 | analyze_handler, rag_handler, cross_doc_handler |

---

### 方案 C（最简 MVP）：ws.py 增量扩展

不改架构，只在现有 ws.py 的 `unified_chat` 路由中逐个添加缺失意图的路由：

```python
# ws.py L370 附近, 在现有 elif 链中追加:
if intent == 'writing':      # 调用 writing 工具
elif intent == 'knowledge':   # 调用 knowledge 工具
elif intent == 'paper_query': # 调用 paper query 工具
elif intent == 'reasoning':   # 调用 cross-paper reasoning 工具
```

**优点**：改动最小，每添加一个意图 3-5 行代码
**缺点**：没有 ReAct 循环、没有 LLM 意图识别、架构依然断裂

**推荐优先级**：P3（备选方案，不建议长期维持）

---

## 六、推荐执行顺序

```
第 1 天 — P0 修复:
  ├── 验证 unified_handler.py 的新版 import 路径全部正确
  ├── 修改 ws.py, 让 unified_chat 路由调用 unified_handler
  ├── 端到端测试：ChatPage 发消息 → 触发写作工具/知识工具
  └── 回滚预案：旧内联路由保留为注释

第 2 天 — P1 修复:
  ├── 前端 useChatMessages.js 订阅 agent_* 事件
  ├── 前端 ChatPage.jsx 渲染 AgentProgress
  ├── 确认 Agent 思考过程在前端可见
  └── 删除 ChatPanel.jsx 的 sendRagMessage 降级

第 3 天 — 清理:
  ├── 确认新版 22 个工具全部正常运行
  ├── 删除旧版 services/agent_service.py
  ├── 删除旧版 agent_handler.py
  ├── 清理 handlers/ 下被替代的旧 handler
  └── 为 22 个工具补充单元测试
```

---

## 七、验证清单

完成修复后，逐项验证：

| # | 验证项 | 预期结果 |
|---|--------|---------|
| 1 | ChatPage 发送问题 → 意图识别 | 21 种意图正确分类 |
| 2 | "写一篇对比分析" → 调用 writing 工具 | 触发 LiteratureReviewTool 并返回结果 |
| 3 | "保存这个知识点" → 调用 knowledge 工具 | 触发 SaveCardTool 并保存 |
| 4 | "我最近读了哪些论文" → 调用 paper_query 工具 | 触发 RecentPapersTool 并返回列表 |
| 5 | "对比这两篇论文的方法论" → 调用 reasoning 工具 | 触发 CrossPaperReasonTool 并返回对比 |
| 6 | 复杂问题 → ReAct 循环 | 触发 react_agent.run() 多步推理 |
| 7 | Agent 思考过程在前端可见 | AgentProgress 组件正常渲染 |
| 8 | 关闭旧版 agent_service.py → 系统正常运行 | 无导入错误 |

---

## 八、总结

| 指标 | 数值 |
|------|------|
| Agent 代码总量 | ~3000+ 行（含 ReAct/意图/规划/22 工具） |
| 实际运行时生效 | ~5% |
| 修复核心改动量 | 1 个文件, ~20 行 |
| 全量修复工期 | 3 天 |
| 主要风险 | unified_handler 的新版 import 路径验证 |
