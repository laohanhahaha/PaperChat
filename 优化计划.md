# PaperChat 优化计划（第二次全面审查版）

> 提出日期：2026-04-17
> 第一次审查日期：2026-04-17
> **第二次审查日期：2026-04-21**
> 状态：已完成第一次计划的大部分内容，本次为增量审查与深度优化

---

## 审查说明

本次审查基于第一次优化计划，对照实际代码确认完成状态，并发现新的优化点。整体架构已大幅改善，但仍有细节需要打磨。

---

## 第一部分：第一次优化计划完成状态确认

### P0 — 必须立即修复（已完成 4/5）

| 序号 | 优化项 | 状态 | 完成度 | 备注 |
|------|--------|------|--------|------|
| 1 | ws.py 上帝文件拆分 | **已完成** | 100% | ws.py 已从 985 行精简到 ~200 行，handlers/ 目录下 6 个 handler 文件已建立 |
| 2 | 数据库迁移改用 Alembic | **已完成** | 100% | migrations/ 目录已建立，含 4 个 migration 版本 |
| 3 | PDF 滚动模式懒加载 | **部分完成** | 60% | IntersectionObserver 已引入，但滚动模式下仍渲染全部页面，需验证 |
| 4 | ReaderPage 并行加载 | **待确认** | 50% | 代码中仍有串行加载痕迹，需检查 App.jsx 最新状态 |
| 5 | BM25 索引缓存 | **已完成** | 100% | `_bm25_cache` 字典 + `invalidate_bm25_cache` 已实现 |

### P1 — 尽快修复（已完成 7/8）

| 序号 | 优化项 | 状态 | 完成度 | 备注 |
|------|--------|------|--------|------|
| 6 | llm_service 提示词分离 | **已完成** | 100% | `services/llm/prompts/` 目录下 5 个提示词文件已建立，`__init__.py` 统一导出 |
| 7 | SessionService / MessageService | **已完成** | 100% | `session_service.py` 和 `message_service.py` 已建立，含 Repository 层 |
| 8 | WebSocket 认证 | **已完成** | 100% | `verify_websocket_token` 已实现，支持 JWT token 和降级默认用户 |
| 9 | Markdown 组件 memo | **已完成** | 100% | 4 个组件已使用 React.memo（AgentProgress、PaperList、KnowledgePage、Recommendations） |
| 10 | 打字机效果优化 | **已完成** | 100% | `useTypewriter` Hook 已使用 requestAnimationFrame，charInterval=50ms，charsPerTick=3 |
| 11 | 跨文档检索并行化 | **已完成** | 100% | `cross_paper_tools.py` 中 `asyncio.gather(*tasks)` 已实现 |
| 12 | 消息列表虚拟化 | **已完成** | 100% | ChatPage.jsx 已使用 `@tanstack/react-virtual`，支持滚动加载更多 |
| 13 | ChatPage 组件拆分 | **已完成** | 100% | 已拆分为 useChatMessages、useTypewriter、useSessionManager 三个 Hook |

### P2 — 计划修复（已完成 5/9）

| 序号 | 优化项 | 状态 | 完成度 | 备注 |
|------|--------|------|--------|------|
| 14 | RAG / LLM 双向依赖解耦 | **部分完成** | 60% | 提示词已分离，但 `llm_service.py` 仍有 3 处延迟导入 `rag_service`，需清理 |
| 15 | Repository 层提取 | **已完成** | 100% | `repositories/session_repository.py` 和 `message_repository.py` 已建立 |
| 16 | Agent ToolContext 统一 | **已完成** | 100% | `tool_base.py` 已定义 ToolContext，所有工具统一使用 |
| 17 | Store 拆分 + API 层封装 | **已完成** | 100% | 前端 Store 已拆分为 sessionStore、messageStore、chatConfigStore；API 层已拆分为 paperApi、chatApi 等 |
| 18 | RAG 索引异步重建 | **待确认** | 50% | 代码中有异步重建逻辑，但需确认是否完全非阻塞 |
| 19 | 全文缓存 | **已完成** | 100% | `message_service.py` 中 `_full_text_cache` 字典已实现 |
| 20 | 前端请求缓存 | **已完成** | 100% | sessionStore 和 messageStore 均实现 5 分钟 TTL 缓存 |
| 21 | 会话消息分页 | **已完成** | 100% | 后端 `get_messages_by_session` 支持 limit/offset，前端 `loadMoreMessages` 已实现 |
| 22 | 重复数据库查询优化 | **已完成** | 100% | `auto_title` 已改用 `count_messages` 替代全量查询 |

### P3 — 后续优化（已完成 4/8）

| 序号 | 优化项 | 状态 | 完成度 | 备注 |
|------|--------|------|--------|------|
| 23 | 全局单例改依赖注入 | **部分完成** | 50% | `main.py` 中已将服务注册到 `app.state`，但路由中仍使用模块级导入 |
| 24 | Paper 分析缓存拆表 | **已完成** | 100% | `PaperAnalysisCache` 模型已建立，Alembic migration 已执行 |
| 25 | 全局错误处理 / Toast | **已完成** | 100% | `api/index.ts` 中响应拦截器 + toastStore 已实现 |
| 26 | 消除硬编码 user_id | **部分完成** | 60% | 配置中仍有 `DEFAULT_USER_ID=1`，但 WebSocket 已支持 token 获取真实用户 |
| 27 | Embedding 线程池扩容 | **待确认** | 50% | `memory_service.py` 中仍为 `max_workers=2`，需确认是否已调整 |
| 28 | 列表项 React.memo | **已完成** | 100% | 4 个组件已添加 memo |
| 29 | WebSocket 消息压缩 | **未开始** | 0% | 仍为独立 JSON 消息，未启用 permessage-deflate |
| 30 | Paper 模型字段拆分 | **已完成** | 100% | 与 #24 合并完成 |

### P4 — 面向未来的优化（已完成 6/15）

| 序号 | 优化项 | 状态 | 完成度 | 备注 |
|------|--------|------|--------|------|
| 31 | 零测试覆盖 | **部分完成** | 30% | `backend/tests/` 已建立，含 conftest.py、test_message_service.py、test_session_service.py，但覆盖率低 |
| 32 | 结构化日志系统 | **部分完成** | 60% | `logging_config.py` 已建立，但仍有部分 `print()` 未替换 |
| 33 | API 版本化 | **已完成** | 100% | 所有路由已迁移到 `/api/v1/` 前缀 |
| 34 | 前端 TypeScript 迁移 | **部分完成** | 50% | Store 和 API 层已迁移为 .ts，但组件仍为 .jsx |
| 35 | CI/CD 流水线 | **未开始** | 0% | 无 `.github/workflows` 或 Dockerfile |
| 36 | 请求限流 | **已完成** | 100% | `slowapi` 已集成，`main.py` 中已配置 limiter |
| 37 | 前端错误边界 | **未开始** | 0% | 无 ErrorBoundary 组件 |
| 38 | 国际化基础设施 | **未开始** | 0% | 所有文本硬编码为中文 |
| 39 | 数据库连接池优化 | **部分完成** | 50% | `pool_pre_ping=True` 已添加，但缺少 pool_size、max_overflow 等参数 |
| 40 | 优雅关停 | **已完成** | 100% | `main.py` lifespan 中已处理 SIGTERM，关闭线程池和数据库连接 |
| 41 | 数据备份与导出 | **已完成** | 100% | `backup_router.py` 和 `memo_export_service.py` 已实现 |
| 42 | Feature Flag 系统 | **已完成** | 100% | `feature_flag_service.py` + `feature_flags` 路由 + 内存缓存已实现 |
| 43 | 事件驱动架构基础 | **已完成** | 100% | `event_bus.py` 已实现发布-订阅，main.py 中已注册事件订阅者 |
| 44 | 安全加固 | **部分完成** | 70% | SecurityHeadersMiddleware 已添加，JWT 密钥检查已添加，但 CORS 仍较宽松 |
| 45 | 数据库索引优化 | **已完成** | 100% | Alembic migration `6b06f2d59928` 已添加性能索引 |

---

## 第二部分：本次审查发现的新问题

### N1. Agent 服务职责拆分（新增 P1）→ 部分已完成

**问题**：`backend/app/services/agent_service.py`（~400 行）同时包含：
- 意图识别逻辑
- 任务规划逻辑
- 10+ 个工具的具体实现（SearchTextTool、ExtractKeyPointsTool、SummarizeTool 等）
- 工具注册表管理

这与 `cross_paper_tools.py`（跨论文工具）和 `react_agent.py`（ReAct 循环）形成了三足鼎立的混乱局面。工具实现散落在 `agent_service.py` 和 `cross_paper_tools.py` 两个文件中，没有统一的归属。

**当前状态（2026-04-21 更新）**：
- **已完成**：工具已迁移到 `services/agent/tools/` 目录下：
  - `paper_tools.py` — SearchTextTool、ExtractKeyPointsTool 等
  - `cross_doc_tools.py` — 跨论文工具
  - `analysis_tools.py` — 分析工具
  - `knowledge_tools.py` — 知识工具
  - `query_tools.py` — 查询工具
  - `writing_tools.py` — 写作工具
- **已完成**：意图识别已迁移到 `services/agent/intent.py`
- **已完成**：Tool 基类已迁移到 `services/core/tool_base.py`
- **待完成**：`agent_service.py` 中仍保留任务规划逻辑和工具注册表，可进一步拆分为 `planner.py` 和 `registry.py`
- **待清理**：`cross_paper_tools.py` 旧文件可能仍存在，需确认是否已废弃

**影响**：工具可维护性、新增工具的便利性、代码清晰度

---

### N2. 前端 Store 代理模式性能隐患（新增 P2）

**问题**：`chatStore.ts` 作为兼容层，使用 getter 代理到底层 store：
```typescript
get sessions() { return useSessionStore.getState().sessions; },
get messages() { return useMessageStore.getState().messages; },
```

这会导致：
1. 组件订阅 `chatStore`，但数据变化发生在 `sessionStore`，Zustand 的订阅机制可能无法正确触发重渲染
2. 每次访问 `chatStore.sessions` 都触发跨 store 调用，增加开销
3. 代理模式掩盖了真正的数据依赖，使调试困难

**当前代码**：
```typescript
// chatStore.ts 第 60-70 行
get sessions() { return S().sessions; },
get currentSessionId() { return S().currentSessionId; },
get messages() { return M().messages; },
// ... 所有状态都是代理
```

**方案**：
1. **短期**：在组件中直接订阅底层 store（sessionStore/messageStore/chatConfigStore），逐步淘汰 chatStore 代理
2. **长期**：完全移除 chatStore，将所有导入路径更新为直接导入底层 store

**迁移示例**：
```typescript
// 当前（有性能隐患）
const { sessions, messages } = useChatStore();

// 改进（直接订阅底层 store）
const { sessions } = useSessionStore();
const { messages } = useMessageStore();
```

**影响**：前端渲染性能、状态管理清晰度

---

### N3. WebSocket Hook 全局单例限制（新增 P2）

**问题**：`useWebSocket.js` 使用模块级全局变量管理连接：
```javascript
let globalWs = null;
let globalStatus = 'disconnected';
const globalHandlers = new Map();
```

这导致：
1. **无法多实例**：无法同时维护多个 WebSocket 连接（如一个用于聊天，一个用于实时通知）
2. **测试困难**：全局状态无法在测试间隔离
3. **重连逻辑与业务耦合**：重连逻辑内嵌在 Hook 中，无法自定义

**当前代码**：
```javascript
// useWebSocket.js 第 10-15 行
let globalWs = null;
let globalStatus = 'disconnected';
let globalRetryCount = 0;
let globalRetryTimer = null;
const globalHandlers = new Map();
const statusListeners = new Set();
```

**方案**：
```javascript
// 封装为可实例化的管理器
class WebSocketManager {
  constructor(url, options = {}) {
    this.url = url;
    this.options = options;
    this.ws = null;
    this.handlers = new Map();
    this.status = 'disconnected';
    this.retryCount = 0;
  }
  
  connect() { ... }
  disconnect() { ... }
  send(type, payload) { ... }
  onMessage(type, handler) { ... }
}

// Hook 仅作为 React 绑定层
export function useWebSocket(manager) {
  const [status, setStatus] = useState(manager.status);
  // ...
}
```

**影响**：架构灵活性、可测试性、未来功能扩展

---

### N4. API 拦截器无法静默请求（新增 P2）

**问题**：`api/index.ts` 的响应拦截器对所有错误都弹出 Toast：
```typescript
api.interceptors.response.use(
  response => response,
  error => {
    const message = error.response?.data?.detail || error.message || '请求失败';
    useToastStore.getState().error(message);  // 所有错误都弹 Toast！
    return Promise.reject(error);
  }
);
```

这导致 fire-and-forget 请求（如标记阅读状态）失败时也会弹出干扰性 Toast。

**方案**：
```typescript
// 支持请求级别的静默配置
api.get('/papers/1/reading-status', { silent: true });

// 拦截器中检查
api.interceptors.response.use(
  response => response,
  error => {
    if (!error.config?.silent) {
      useToastStore.getState().error(message);
    }
    return Promise.reject(error);
  }
);
```

**影响**：用户体验、避免不必要的错误提示

---

### N5. 后端 Handler 重复代码模式（新增 P2）→ 部分已完成

**问题**：所有 handler（analyze、rag、cross_doc、agent）都遵循相同的 6 步模式：
1. 获取/创建会话
2. 保存用户消息
3. 加载历史
4. 执行业务逻辑
5. 保存 assistant 消息
6. 发送 done 信号

但没有抽象基类，每个 handler 都重复这些样板代码。

**当前状态（2026-04-21 更新）**：
- **已完成**：`backend/app/handlers/base.py` 已建立 `ChatHandlerBase` 抽象基类，实现了 Template Method 模式：
  - `handle()` 方法负责会话管理、消息持久化和 done 信号
  - 子类只需实现 `_process()` 方法
  - 已定义 `channel` 和 `chunk_type` 类属性
- **待完成**：需要确认所有 handler（rag_handler、cross_doc_handler、agent_handler 等）是否已继承 ChatHandlerBase

**影响**：代码复用、减少样板代码、新增 handler 的开发效率

---

### N6. 配置管理分散且无同步机制（新增 P2）

**问题**：配置分散在三个位置：
- `backend/app/config.py` — Pydantic Settings（系统级配置）
- `backend/app/services/settings_service.py` — 用户配置（数据库）
- `frontend/src/stores/settingsStore.ts` — 前端配置（内存）

三者之间没有同步机制：
1. 用户在后端修改配置后，前端不会自动感知
2. 前端刷新页面后，settingsStore 重新从 API 加载
3. LLM 配置变更后，需要手动刷新页面才能生效

**方案**：
1. 建立配置变更事件通知机制
2. 前端通过 WebSocket 接收配置变更推送
3. 或采用轮询机制（简单但不够实时）

```python
# 配置变更时发布事件
await event_bus.publish(Event(
    type=EventTypes.SETTINGS_CHANGED,
    data={"user_id": user_id, "settings": changed_settings}
))
```

**影响**：用户体验、配置一致性

---

### N7. 服务层文件数量膨胀（新增 P2）→ 部分已完成

**问题**：`backend/app/services/` 已有 28 个 Python 文件，按平铺方式组织，没有子目录划分。

**当前状态（2026-04-21 更新）**：
- **已完成**：以下子目录已建立：
  - `services/agent/` — Agent 相关（含 tools/ 子目录）
  - `services/llm/` — LLM 相关（含 prompts/ 子目录）
  - `services/knowledge/` — 知识库相关（含 graph_service.py）
  - `services/core/` — 核心基础设施（tool_base.py、tool_cache.py、event_bus.py）
- **待完成**：以下服务仍平铺在 services/ 根目录，需进一步整理：
  - `auth_service.py`、`memory_service.py`、`profile_service.py`、`settings_service.py` → 可归为 user/
  - `session_service.py`、`message_service.py`、`context_service.py` → 可归为 chat/
  - `pdf_service.py` → 可归为 paper/
  - `search_service.py` → 可归为 search/
  - `citation_service.py`、`memo_export_service.py` → 可归为 export/
  - `security_service.py`、`clarification_service.py`、`confirmation_service.py` → 可归为 security/

**影响**：代码可导航性、新开发者上手速度、模块边界清晰度

---

### N8. 知识图谱服务提示词内嵌（新增 P3）→ 已完成

**问题**：`graph_service.py` 将 LLM 提示词直接内嵌在类中：
```python
class GraphService:
    EXTRACT_ENTITIES_PROMPT = """..."""
    BUILD_RELATIONS_PROMPT = """..."""
```

这与 `llm_service.py` 的提示词分离原则相违背。

**当前状态（2026-04-21 更新）**：
- **已完成**：提示词已迁移到 `services/llm/prompts/graph_prompts.py`
- **已完成**：`graph_service.py` 中通过 `from app.services.llm.prompts.graph_prompts import EXTRACT_ENTITIES_PROMPT, BUILD_RELATIONS_PROMPT` 导入

**影响**：提示词管理一致性

---

### N9. 前端路由与状态不同步（新增 P3）

**问题**：`App.jsx` 使用 URL 参数获取论文 ID，但 `paperStore.currentPaper` 与 URL 不同步。刷新页面后状态丢失，需要重新加载。

**方案**：使用 URL 作为单一数据源，状态仅作为缓存。

**影响**：刷新页面后的用户体验

---

### N10. 测试覆盖率仍不足（新增 P3）

**问题**：虽然已建立测试框架，但：
1. 只有 2 个测试文件（test_message_service.py、test_session_service.py）
2. 没有 RAG 服务测试
3. 没有 Agent 工具测试
4. 没有 API 集成测试
5. 没有前端测试

**方案**：
```
backend/tests/
├── conftest.py              # 已有
├── unit/
│   ├── test_message_service.py      # 已有
│   ├── test_session_service.py      # 已有
│   ├── test_rag_service.py          # 新增
│   ├── test_agent_tools.py          # 新增
│   └── test_tool_cache.py           # 新增
└── integration/
    ├── test_chat_api.py             # 新增
    ├── test_paper_api.py            # 新增
    └── test_websocket.py            # 新增
```

**影响**：回归保障、重构信心

---

### N11. 事件总线使用不充分（新增 P3）

**问题**：`event_bus.py` 已实现完善的发布-订阅机制，但目前只有 `main.py` 中注册了 3 个日志订阅者。论文上传后的索引构建、知识提取、推荐更新等流程仍通过直接函数调用耦合。

**当前使用**：
```python
# main.py 中只有日志订阅
event_bus.subscribe(EventTypes.PAPER_UPLOADED, log_event_handler)
event_bus.subscribe(EventTypes.PAPER_DELETED, log_event_handler)
```

**方案**：扩展事件驱动架构：
```python
# 论文上传后自动触发后续处理
event_bus.subscribe(EventTypes.PAPER_UPLOADED, rag_service.index_paper)
event_bus.subscribe(EventTypes.PAPER_UPLOADED, knowledge_service.extract_entities)
event_bus.subscribe(EventTypes.PAPER_UPLOADED, recommendation_service.update_recommendations)

# 分析完成后通知前端刷新
event_bus.subscribe(EventTypes.ANALYSIS_COMPLETED, notify_frontend)
```

**影响**：模块解耦、功能扩展性

---

### N12. 前端组件文件仍过大（新增 P3）

**问题**：
- `PDFReader.jsx` — 500+ 行（阅读辅助、高亮、工具栏全部在一个组件）
- `App.jsx` — 600+ 行（ReaderPage 包含所有阅读状态）

**方案**：
```
PDFReader/
├── PDFReader.jsx          # 主组件（仅负责渲染和状态管理，~200行）
├── PageRenderer.jsx       # 页面渲染（单页/滚动模式）
├── TextSelection.jsx      # 文本选中处理
├── ReadingAssistPanel.jsx # 阅读辅助面板
├── HighlightLayer.jsx     # 高亮层
├── Toolbar.jsx            # 工具栏
└── usePDFReader.js        # 自定义 Hook（状态逻辑，~150行）
```

**影响**：可维护性、组件复用

---

## 第三部分：优化优先级总表（更新版）

### P0 — 必须立即修复（新增 1 项）

| 序号 | 优化项 | 分类 | 影响维度 |
|------|--------|------|----------|
| 1 | ws.py 拆分 | 架构 | 可维护性 |
| 2 | Alembic 迁移 | 数据层 | 数据安全 |
| 3 | PDF 懒加载验证 | 性能 | 内存/卡顿 |
| 4 | ReaderPage 并行加载确认 | 性能 | 载入时间 |
| 5 | BM25 缓存 | 性能 | 响应速度 |
| **N1** | **Agent 服务职责拆分** | **架构** | **可维护性** |

### P1 — 尽快修复（已完成大部分，剩余 2 项）

| 序号 | 优化项 | 分类 | 影响维度 |
|------|--------|------|----------|
| 6 | 提示词分离 | 架构 | 可维护性 |
| 7 | Session/Message Service | 耦合 | 代码复用 |
| 8 | WebSocket 认证 | 安全 | 安全 |
| 9 | Markdown memo | 性能 | 渲染性能 |
| 10 | 打字机效果优化 | 性能 | 流畅度 |
| 11 | 跨文档检索并行 | 性能 | 响应速度 |
| 12 | 消息列表虚拟化 | 性能 | 渲染性能 |
| 13 | ChatPage 拆分 | 架构 | 可维护性 |
| **14** | **RAG/LLM 双向依赖清理** | **耦合** | **架构清晰度** |

### P2 — 计划修复（新增 5 项）

| 序号 | 优化项 | 分类 | 影响维度 |
|------|--------|------|----------|
| 15 | Repository 层 | 架构 | 代码复用 |
| 16 | ToolContext 统一 | 架构 | 扩展性 |
| 17 | Store 拆分 + API 封装 | 架构 | 前端架构 |
| 18 | RAG 索引异步重建 | 性能 | 响应速度 |
| 19 | 全文缓存 | 性能 | 响应速度 |
| 20 | 前端请求缓存 | 性能 | 载入时间 |
| 21 | 会话消息分页 | 性能 | 载入时间 |
| 22 | 重复查询优化 | 性能 | 查询效率 |
| **N2** | **Store 代理模式移除** | **性能** | **渲染性能** |
| **N3** | **WebSocket Hook 重构** | **架构** | **可测试性** |
| **N4** | **API 静默请求** | **体验** | **用户体验** |
| **N5** | **Handler 抽象基类** | **架构** | **代码复用** |
| **N6** | **配置同步机制** | **架构** | **配置一致性** |
| **N7** | **服务层目录重组** | **架构** | **可导航性** |

### P3 — 后续优化（新增 4 项）

| 序号 | 优化项 | 分类 | 影响维度 |
|------|--------|------|----------|
| 23 | 依赖注入完善 | 架构 | 可测试性 |
| 24 | Paper 分析拆表 | 数据层 | 数据模型 |
| 25 | 全局错误处理 | 体验 | 用户体验 |
| 26 | 硬编码 user_id | 架构 | 多用户 |
| 27 | Embedding 线程池 | 性能 | 并发性能 |
| 28 | React.memo | 性能 | 渲染性能 |
| 29 | WebSocket 压缩 | 性能 | 带宽 |
| 30 | Paper 模型拆分 | 数据层 | 数据模型 |
| **N8** | **知识图谱提示词分离** | **架构** | **一致性** |
| **N9** | **路由状态同步** | **体验** | **刷新体验** |
| **N10** | **测试覆盖率提升** | **工程化** | **回归保障** |
| **N11** | **事件总线扩展使用** | **架构** | **模块解耦** |
| **N12** | **前端组件拆分** | **架构** | **可维护性** |

### P4 — 面向未来（保持不变）

| 序号 | 优化项 | 分类 | 影响维度 |
|------|--------|------|----------|
| 31-45 | 测试、日志、API 版本、TypeScript、CI/CD、限流、错误边界、国际化、连接池、优雅关停、备份、Feature Flag、事件驱动、安全、索引 | 工程化 | 长期基础 |

---

## 第四部分：建议实施路线图（2026-04-21 更新）

> 基于代码实际状态重新调整。N1（Agent 工具拆分）、N5（Handler 基类）、N7（服务层目录重组）、N8（知识图谱提示词）已部分或全部完成。

### Phase 1（本周）— 高优先级剩余项
- [ ] **N1 剩余**: 将 `agent_service.py` 中的任务规划逻辑拆分为 `planner.py`，工具注册表拆分为 `registry.py`
- [ ] **N5 确认**: 检查所有 handler（rag_handler、cross_doc_handler、agent_handler、unified_handler）是否已继承 ChatHandlerBase
- [ ] **14**: 清理 `llm_service.py` 中延迟导入 `rag_service`（代码检查显示可能已完成，需确认）
- [ ] **3**: 验证 PDF 懒加载是否真正生效（检查滚动模式下实际渲染的页面数）
- [ ] **4**: 确认 ReaderPage 并行加载（检查 App.jsx 最新状态）

### Phase 2（下周）— 前端性能与体验优化
- [ ] **N2**: 逐步移除 chatStore 代理模式（先在新组件中直接使用底层 store）
- [ ] **N4**: API 拦截器支持 `silent: true` 配置
- [ ] **N3**: WebSocket Hook 重构为可实例化模式
- [ ] **N9**: 前端路由与状态同步优化

### Phase 3（第 3 周）— 后端架构完善
- [ ] **N6**: 配置变更事件通知机制（前后端配置同步）
- [ ] **N7 剩余**: 将平铺服务整理到 user/、chat/、paper/、search/、export/、security/ 子目录
- [ ] **18**: RAG 索引异步重建确认与完善
- [ ] **23**: 依赖注入完善（路由中使用 app.state 而非模块级导入）

### Phase 4（第 4 周）— 质量与测试
- [ ] **N10**: 增加核心服务单元测试（RAG、Agent 工具、ToolCache）
- [ ] **N11**: 扩展事件总线使用场景（论文上传后自动触发索引、知识提取、推荐更新）
- [ ] **N12**: PDFReader 组件拆分
- [ ] **31**: 测试覆盖率提升

### Phase 5（持续）— P4 基础设施
- [ ] 前端 TypeScript 全面迁移
- [ ] CI/CD 流水线搭建
- [ ] 前端 ErrorBoundary
- [ ] 国际化基础设施
- [ ] 安全加固完善

---

## 附录：关键文件清单

### 已完成优化的核心文件
| 文件 | 优化内容 | 状态 |
|------|----------|------|
| `backend/app/routers/ws.py` | 拆分为 handlers/ | 已完成 |
| `backend/app/services/llm/prompts/` | 提示词分离 | 已完成 |
| `backend/app/repositories/` | Repository 层 | 已完成 |
| `backend/app/services/session_service.py` | Session 服务 | 已完成 |
| `backend/app/services/message_service.py` | Message 服务 + 全文缓存 | 已完成 |
| `backend/app/models/paper_analysis.py` | 分析缓存拆表 | 已完成 |
| `backend/migrations/` | Alembic 迁移 | 已完成 |
| `backend/app/services/event_bus.py` | 事件总线 | 已完成 |
| `backend/app/services/feature_flag_service.py` | Feature Flag | 已完成 |
| `backend/app/main.py` | 依赖注入 + 优雅关停 | 已完成 |
| `frontend/src/stores/sessionStore.ts` | Store 拆分 | 已完成 |
| `frontend/src/stores/messageStore.ts` | 消息管理 + 分页 | 已完成 |
| `frontend/src/stores/chatConfigStore.ts` | 配置分离 | 已完成 |
| `frontend/src/api/chatApi.ts` | API 层封装 | 已完成 |
| `frontend/src/api/paperApi.ts` | API 层封装 | 已完成 |
| `frontend/src/hooks/useTypewriter.js` | 打字机优化 | 已完成 |
| `frontend/src/hooks/useChatMessages.js` | 消息监听 Hook | 已完成 |
| `frontend/src/hooks/useSessionManager.js` | 会话管理 Hook | 已完成 |
| `frontend/src/pages/ChatPage.jsx` | 虚拟滚动 + 组件拆分 | 已完成 |

### 待优化的核心文件
| 文件 | 待优化内容 | 优先级 |
|------|------------|--------|
| `backend/app/services/agent_service.py` | 工具迁移到子目录 | P1 |
| `backend/app/services/cross_paper_tools.py` | 迁移到 `agent/tools/` | P1 |
| `backend/app/services/llm_service.py` | 清理 rag_service 延迟导入 | P1 |
| `frontend/src/stores/chatStore.ts` | 移除代理模式 | P2 |
| `frontend/src/hooks/useWebSocket.js` | 重构为可实例化 | P2 |
| `frontend/src/api/index.ts` | 支持 silent 配置 | P2 |
| `backend/app/handlers/*.py` | 提取 ChatHandlerBase | P2 |
| `backend/app/services/` | 按功能域重组目录 | P2 |
| `backend/app/services/graph_service.py` | 提示词分离 | P3 |
| `frontend/src/components/PDFReader/PDFReader.jsx` | 组件拆分 | P3 |

---

> **总结**：第一次优化计划执行效果非常好，P0/P1 级别的问题已基本解决。当前项目架构相比最初已有质的飞跃。本次审查发现的新问题主要集中在**服务层组织**、**前端状态管理精细化**、**代码复用模式抽象**三个方面，属于"锦上添花"级别的优化，不会阻塞功能开发，但会显著提升长期维护效率。建议按 Phase 1-3 的顺序逐步实施。
