# PaperChat 优化计划

> 提出日期：2026-04-17
> 更新日期：2026-04-17

---

## 第一次优化计划总览

本计划涵盖**架构设计**、**模块化**、**耦合分离**和**性能优化**四个维度，按优先级从高到低排列。

---

## P0 — 必须立即修复（严重影响可维护性 / 用户体验 / 数据安全）

### 1. ws.py 上帝文件拆分（985 行）

**问题**：`backend/app/routers/ws.py` 承担了所有 WebSocket 逻辑，包含 7+ 个 handler、数据库操作、LLM 调用、RAG 检索、会话管理、意图识别、消息格式化，一个文件干了 5 个模块的活。

**方案**：

```
routers/ws.py（仅保留连接管理和消息分发，~100行）
├── handlers/
│   ├── analyze_handler.py      # 论文分析
│   ├── rag_handler.py          # RAG 问答
│   ├── cross_doc_handler.py    # 跨文档问答
│   ├── agent_handler.py        # Agent 问答
│   └── unified_handler.py      # 统一入口（意图识别 + 路由）
├── services/
│   ├── session_service.py      # 会话 CRUD（从 ws.py 提取）
│   └── message_service.py      # 消息保存/加载（从 ws.py 提取）
```

**影响**：可维护性、代码可读性、协作效率

---

### 2. 数据库迁移改用 Alembic

**问题**：`backend/app/database.py` 中用 `try/except` 包裹 `ALTER TABLE` 语句做迁移，非常脆弱：

```python
alter_statements = [
    "ALTER TABLE papers ADD COLUMN section_analysis TEXT",
    "ALTER TABLE papers ADD COLUMN deep_analysis TEXT",
    ...
]
for stmt in alter_statements:
    try:
        async with AsyncSessionLocal() as db:
            await db.execute(text(stmt))
            await db.commit()
    except Exception:
        pass  # 列已存在，忽略错误
```

**方案**：

```bash
pip install alembic
alembic init migrations
alembic revision --autogenerate -m "add analysis fields"
alembic upgrade head
```

**影响**：数据安全、迁移可靠性、生产环境风险

---

### 3. PDF 滚动模式渲染所有页面

**问题**：`frontend/src/components/PDFReader/PDFReader.jsx` 在滚动模式下一次性渲染所有页面，20 页论文 = 20 个 Canvas + TextLayer + AnnotationLayer，占用 200MB+ 内存。

```jsx
return Array.from({ length: numPages || 0 }, (_, i) => i + 1).map((pageNum) => {
  return <Page pageNumber={pageNum} ... />;  // 全部同时渲染
});
```

**方案**：使用 Intersection Observer 实现懒加载，只渲染可视区域 ± 2 页

**影响**：阅读长论文时卡顿 / 内存溢出，预期内存降 80%

---

### 4. ReaderPage 瀑布式加载 — 6 个串行请求

**问题**：`frontend/src/App.jsx` 的 `loadPaper` 函数串行执行 6+ 个异步操作，每个 200ms，总等待 ~1.6 秒。

```javascript
const paper = await fetchPaper(id);           // 1
await markAsReading(id);                       // 2
const textData = await fetchPaperText(id);     // 3
const res = await api.get(`/papers/${id}/analysis`); // 4
await fetchHighlights(id);                     // 5
await fetchNotes(id);                          // 6
fetchRecommendations(id);                      // 7
```

**方案**：并行化独立请求

```javascript
const paper = await fetchPaper(id);
const [, textData, analysisRes] = await Promise.all([
  markAsReading(id).catch(console.warn),
  fetchPaperText(id),
  api.get(`/papers/${id}/analysis`).catch(() => null),
]);
await Promise.all([
  fetchHighlights(id),
  fetchNotes(id),
  fetchRecommendations(id).catch(console.error),
]);
```

**影响**：进入阅读页白屏 1.6s → 降至 0.5s

---

### 5. BM25 索引每次检索都重建

**问题**：`backend/app/services/rag_service.py` 的 `search` 方法每次都从 ChromaDB 取出全部文档，重新构建 BM25 索引（磁盘 I/O + CPU 密集），重复检索同一论文时浪费 ~450ms。

**方案**：缓存 BM25 索引

```python
class RAGService:
    def __init__(self):
        self._bm25_cache = {}  # paper_id -> (bm25_instance, all_docs)

    def _get_bm25(self, paper_id, collection):
        if paper_id in self._bm25_cache:
            return self._bm25_cache[paper_id]
        all_docs = collection.get()
        tokenized = [doc.split() for doc in all_docs["documents"]]
        bm25 = BM25Okapi(tokenized)
        self._bm25_cache[paper_id] = (bm25, all_docs)
        return bm25, all_docs
```

**影响**：重复检索从 ~500ms 降至 ~50ms

---

## P1 — 尽快修复（明显影响开发效率 / 用户体验）

### 6. llm_service.py 提示词与逻辑混杂（1057 行）

**问题**：`backend/app/services/llm_service.py` 包含 12+ 个提示词常量（占 300+ 行）和 LLM 调用逻辑混在一起，提示词修改需要改 Python 代码，无法热更新。

**方案**：

```
services/llm/
├── prompts/                  # 提示词独立管理
│   ├── analyze.py
│   ├── chat.py
│   ├── rag.py
│   ├── reading.py
│   ├── writing.py
│   └── agent.py
├── llm_service.py            # 核心 LLM 调用逻辑（~200行）
└── prompt_manager.py         # 提示词加载/模板渲染
```

**影响**：可维护性，未来可支持提示词热更新

---

### 7. 提取 SessionService / MessageService

**问题**：WebSocket 路由中直接使用 `AsyncSessionLocal()` 手动管理数据库会话（出现 9 次），绕过了 FastAPI 的依赖注入体系。会话 CRUD 和消息保存逻辑散落在 ws.py 各处。

**方案**：

```python
class SessionService:
    async def get_or_create(self, db, session_id, user_id, paper_id=None) -> ChatSession: ...
    async def auto_title(self, db, session_id, first_message) -> None: ...

class MessageService:
    async def save(self, db, session_id, role, content, sources=None) -> ChatMessage: ...
    async def load_history(self, db, session_id, limit=10) -> ChatMessageHistory: ...
    async def count(self, db, session_id) -> int: ...
```

**影响**：耦合度、代码复用、可测试性

---

### 8. WebSocket 缺少认证

**问题**：`ws.py` 的 WebSocket 端点没有任何认证，任何人都可以连接。

**方案**：

```python
@router.websocket("/ws")
async def ws_endpoint(websocket: WebSocket, token: str = Query(...)):
    user = await verify_token(token)
    if not user:
        await websocket.close(code=4001, reason="Unauthorized")
        return
    await websocket.accept()
```

**影响**：安全

---

### 9. Markdown 组件无 React.memo

**问题**：`frontend/src/utils/MarkdownRenderer.jsx` 的 `MarkdownContent` 没有 `React.memo`，打字机效果每 30ms 更新一次时，所有消息的 Markdown 都被重新解析渲染。

**方案**：

```jsx
export const MarkdownContent = React.memo(function MarkdownContent({ content, className }) {
  if (!content) return null;
  return (
    <div className={className}>
      <ReactMarkdown components={components}>{content}</ReactMarkdown>
    </div>
  );
});
```

**影响**：流式输出时 CPU 降 70%

---

### 10. 打字机效果 30ms setState 过频

**问题**：`ChatPage.jsx` 和 `ChatPanel.jsx` 的打字机效果每 30ms 触发一次 `updateLastMessage`，每次都创建新数组拷贝，30ms 间隔 = 每秒 33 次 setState + 33 次全消息列表重渲染。

**方案**：

1. `CHAR_INTERVAL` 从 30ms 提升到 50ms，每次多显示几个字符
2. 使用 `requestAnimationFrame` 替代 `setTimeout`，与浏览器刷新率同步
3. 累积 2-3 个 chunk 后再更新一次

**影响**：长回答时 UI 卡顿，流畅度提升明显

---

### 11. 跨文档检索串行执行

**问题**：`rag_service.py` 的 `search_multiple_papers` 串行检索每篇论文，3 篇论文 = 3 × 500ms = 1.5 秒。

```python
for paper_id in paper_ids:          # 串行！
    results = await self.search(paper_id, query, top_k=3)
```

**方案**：

```python
tasks = [self.search(pid, query, top_k=3) for pid in paper_ids]
results_list = await asyncio.gather(*tasks)
```

**影响**：3 篇论文检索从 1.5s 降至 ~500ms

---

### 12. 消息列表无虚拟化

**问题**：`ChatPage.jsx` 和 `ChatPanel.jsx` 的消息列表直接 `.map()` 渲染所有消息，每条都包含 `ReactMarkdown` 解析。50+ 条消息时每次流式更新都触发全部消息重渲染。

**方案**：使用 `@tanstack/react-virtual` 虚拟列表，只渲染可视区域内的消息

**影响**：50+ 条消息时渲染时间从 ~200ms 降至 ~10ms

---

### 13. 前端 ChatPage 拆分（632 行）

**问题**：`ChatPage.jsx` 中 WebSocket 消息监听、打字机动画、会话管理、意图展示全部写在一个组件里，7 个 `useEffect`、6 个 ref。

**方案**：

```
hooks/
├── useWebSocket.js          # 连接管理（已有）
├── useChatStream.js         # 流式消息 + 打字机效果（新提取）
├── useSessionManager.js     # 会话 CRUD（新提取）
└── useIntentDetection.js    # 意图识别展示（新提取）

ChatPage.jsx → 组合以上 hooks，仅负责渲染（~200行）
```

**影响**：可维护性、代码复用

---

## P2 — 计划修复（改善架构质量 / 中等性能提升）

### 14. RAG / LLM 双向依赖解耦

**问题**：`llm_service.py` 内部调用 `rag_service`（3 处延迟导入），而 `agent_service.py` 的工具类也调用 `rag_service`。LLM 层不应该知道 RAG 层的存在。

**方案**：

```
当前：Handler → LLMService → RAGService（LLM 知道 RAG）
改进：Handler → RAGService + LLMService（Handler 编排两者）
```

**影响**：架构清晰度、可替换性

---

### 15. Repository 层提取

**问题**：数据库查询散落在路由和 WebSocket handler 中，`chat.py` 和 `ws.py` 中有大量 `select`/`where`/`order_by` 查询逻辑。

**方案**：

```python
class SessionRepository:
    async def get_by_user(self, db, user_id, paper_id=None) -> list[ChatSession]: ...
    async def get_by_id(self, db, session_id, user_id) -> ChatSession: ...
    async def create(self, db, user_id, paper_id=None, title="新对话") -> ChatSession: ...
    async def delete(self, db, session_id, user_id) -> bool: ...

class MessageRepository:
    async def get_by_session(self, db, session_id, limit=100) -> list[ChatMessage]: ...
    async def save(self, db, session_id, role, content, sources=None) -> ChatMessage: ...
```

**影响**：代码复用、可测试性

---

### 16. Agent ToolContext 统一

**问题**：`agent_service.py` 中的工具类 `execute` 方法参数签名不统一，有的需要 `db`，有的需要 `paper_id`，有的需要 `text`。`GetPaperInfoTool.execute` 甚至需要传入 `db` 参数，破坏了工具的无状态性。

**方案**：

```python
@dataclass
class ToolContext:
    db: AsyncSession
    paper_id: Optional[int] = None
    paper_ids: Optional[list[int]] = None
    user_id: Optional[int] = None

class Tool(ABC):
    @abstractmethod
    async def execute(self, ctx: ToolContext, **kwargs) -> dict: ...
```

**影响**：扩展性、工具一致性

---

### 17. Store 拆分 + API 层封装

**问题 A**：`chatStore.js`（416 行）混合了会话 CRUD、消息管理、跨文档状态、联网搜索状态、意图检测状态、WebSocket 消息发送。

**问题 B**：`api/index.js` 只有 11 行，一个裸的 axios 实例，所有 API 调用散落在各个 store 中。

**方案 A**：

```
stores/
├── sessionStore.js      # 会话 CRUD + 列表
├── messageStore.js      # 消息管理 + 流式更新
├── chatConfigStore.js   # 联网搜索/跨文档/意图等配置
└── paperStore.js        # 保持不变
```

**方案 B**：

```
api/
├── index.js          # axios 实例 + 拦截器
├── paperApi.js       # 论文相关 API
├── chatApi.js        # 会话/消息 API
├── knowledgeApi.js   # 知识库 API
└── writingApi.js     # 写作辅助 API
```

**影响**：前端架构清晰度、代码复用

---

### 18. RAG 无索引时同步重建阻塞问答

**问题**：`ws.py` 当 RAG 检索无结果时，在问答流程中同步重建索引，可能阻塞 5-10 秒。

**方案**：
1. 论文上传时就建立索引（当前已有，但可能失败）
2. 运行时发现无索引 → 返回提示让用户稍后重试，后台异步重建
3. 添加索引状态检查 API，前端在进入阅读页时预检查

**影响**：用户等待 5-10s → 改为异步提示

---

### 19. 全文每次从数据库取出

**问题**：`ws.py` 的 `get_paper_full_text` 每次分析都从数据库取出论文全部文本块并拼接，20 页论文可能有 3-5 万字符。

**方案**：缓存全文到 Redis 或内存，论文上传/修改时更新缓存

**影响**：分析/问答响应速度

---

### 20. 前端无请求缓存

**问题**：每次切换会话都重新 `fetchMessages`，每次进入论文页面都重新 `fetchPaper`，没有客户端缓存。

**方案**：使用 Zustand 持久化中间层 + 简单的内存缓存（5 分钟过期）

**影响**：重复加载相同数据时秒开

---

### 21. 会话消息无分页

**问题**：`chatStore.js` 的 `fetchMessages` 一次加载全部消息，后端也没有分页参数。长对话（100+ 条消息）加载很慢。

**方案**：后端添加 `limit` 和 `offset` 参数，前端滚动到顶部时加载更多

**影响**：长对话加载速度

---

### 22. WebSocket handler 中重复数据库查询

**问题**：`ws.py` 的 `handle_rag_chat` 保存消息后又查询全部消息来判断是否需要自动命名，取出所有数据只为了看数量。

```python
result = await db.execute(select(ChatMessage).where(...))
all_messages = result.scalars().all()  # 取出全部，只看 len
```

**方案**：用 `COUNT` 查询代替

```python
count = await db.execute(select(func.count(ChatMessage.id)).where(...))
if count.scalar() <= 2 and session.title == "新对话":
    session.title = message[:30] + "..."
```

**影响**：数据库查询效率

---

## P3 — 后续优化（提升工程质量 / 小幅性能提升）

### 23. 全局单例改依赖注入

**问题**：`rag_service`、`llm_service`、`agent_service` 都是模块级全局单例，无法替换、无法测试、无法配置不同实例。

**方案**：

```python
# dependencies.py
def get_llm_service() -> LLMService:
    return llm_service

# 路由中使用
@router.websocket("/ws")
async def ws_endpoint(websocket: WebSocket, llm: LLMService = Depends(get_llm_service)):
    ...
```

**影响**：可测试性、未来多租户支持

---

### 24. Paper 分析缓存拆表

**问题**：`Paper` 模型把分析缓存（`section_analysis`、`deep_analysis`、`analysis_status`）直接放在论文表里，随着功能增加表会越来越臃肿。

**方案**：

```python
class PaperAnalysis(Base):
    paper_id = ForeignKey("papers.id")
    analysis_type = Column(String)  # "section" | "deep" | "quality"
    content = Column(Text)
    status = Column(String)
    created_at = Column(DateTime)
```

**影响**：数据模型扩展性

---

### 25. 全局错误处理 / Toast 通知

**问题**：每个 store 独立管理 `loading` 和 `error` 状态，组件中到处是 `try/catch` + `set({ error: ... })`，没有全局的错误提示机制。

**方案**：在 `api/index.js` 添加响应拦截器，统一 toast 通知

**影响**：用户体验、代码简洁度

---

### 26. 消除硬编码 user_id=1

**问题**：`main.py` 启动时加载 `user_id=1` 的配置，`database.py` 初始化时创建 `id=1` 的默认用户，整个系统假设只有一个用户。

**方案**：配置应关联到当前登录用户，而非硬编码

**影响**：多用户支持

---

### 27. Embedding 线程池仅 2 worker

**问题**：`rag_service.py` 的线程池只有 2 个 worker，多用户同时检索时排队等待。

**方案**：

```python
self._executor = ThreadPoolExecutor(max_workers=min(os.cpu_count() or 4, 8))
```

**影响**：并发检索性能

---

### 28. 列表项无 React.memo

**问题**：整个前端代码库没有任何 `React.memo` 的使用，当 `sessions` 或 `messages` 更新时，所有列表项都重新渲染。

**方案**：对会话列表项、消息气泡、论文卡片、推荐卡片、知识卡片添加 `React.memo`

**影响**：列表更新时按需渲染

---

### 29. WebSocket 消息无压缩

**问题**：流式输出的每个 chunk 都是独立 JSON 消息，高频消息（打字机效果）带宽开销较大。

**方案**：
1. 对连续 chunk 使用增量更新格式
2. 启用 WebSocket 的 `permessage-deflate` 压缩扩展

**影响**：带宽降 30%

---

### 30. Paper 模型字段膨胀

**问题**：`Paper` 模型中 `section_analysis`、`deep_analysis`、`analysis_status`、`last_analyzed_at` 等分析缓存字段直接放在论文表里，与论文核心数据混杂。

**方案**：与第 24 条合并，拆分为独立的 `PaperAnalysis` 表

**影响**：数据模型清晰度

---

## P4 — 面向未来的优化（为后续功能扩展做准备）

> 以下优化项不针对当前痛点，而是为后续新功能（多 Agent 协作、Tauri 桌面端、多用户等）铺路。越早建设，后续开发越顺畅。

### 31. 零测试覆盖 — 全项目无任何测试文件

**问题**：整个项目没有任何单元测试、集成测试或 E2E 测试文件（`test_*`、`*.test.*` 均为空）。当前靠手动验证，后续功能增多后回归风险极高。

**方案**：

```
backend/tests/
├── conftest.py              # pytest fixtures（db session, test client）
├── test_rag_service.py      # RAG 检索测试
├── test_agent_service.py    # Agent 工具测试
├── test_session_service.py  # 会话管理测试
├── test_chat_api.py         # 聊天 API 集成测试
└── test_paper_api.py        # 论文 API 集成测试

frontend/src/
├── __tests__/
│   ├── stores/
│   │   ├── chatStore.test.js
│   │   └── paperStore.test.js
│   └── components/
│       └── MarkdownRenderer.test.jsx
```

**优先建设**：后端核心服务（RAG、Agent）的单元测试，使用 `pytest-asyncio` + mock LLM 调用

**影响**：回归保障、重构信心、后续功能开发速度

---

### 32. 日志系统 — 后端 45 处 print() 替换为结构化日志

**问题**：后端有 **45 处** `print()` 调用分布在 12 个文件中，仅 3 个文件使用了 `logging`。生产环境无法按级别过滤、无法收集、无法追踪请求链路。

```python
# 当前：散落的 print
print(f"检索论文 {paper_id} 失败: {e}")
print(f"[WARN] 论文 {paper_id} 关键词提取超时")

# 改进：结构化日志
logger.error("rag_search_failed", paper_id=paper_id, error=str(e))
logger.warning("keyword_extraction_timeout", paper_id=paper_id, timeout=15)
```

**方案**：

1. 统一使用 `logging` 模块，配置 JSON 格式输出
2. 添加请求追踪 ID（correlation ID），串联前后端日志
3. 配置日志级别（开发 DEBUG、生产 INFO）
4. 关键路径添加耗时日志（RAG 检索、LLM 调用、索引构建）

**影响**：线上问题排查效率、性能瓶颈定位

---

### 33. API 版本化

**问题**：所有 API 路由直接挂在 `/api/` 下，没有版本号：

```python
router = APIRouter(prefix="/api/chat")      # 当前
router = APIRouter(prefix="/api/papers")     # 当前
```

后续如果需要修改 API 响应格式或参数，无法兼容旧客户端。

**方案**：

```python
# 方案一：URL 前缀版本化
router = APIRouter(prefix="/api/v1/chat")
router = APIRouter(prefix="/api/v1/papers")

# 方案二：Header 版本化（更灵活）
# Accept: application/vnd.paperchat.v1+json
```

**影响**：API 兼容性、多客户端支持（Web + Tauri + 未来移动端）

---

### 34. 前端 TypeScript 迁移

**问题**：前端全部使用 JavaScript，没有类型检查。随着功能增多（多 Agent、知识图谱、写作辅助），状态管理复杂度急剧上升，类型错误只能在运行时发现。

**方案**：

1. 先添加 `jsconfig.json` 路径别名（已有）
2. 逐步将 Store 和 API 层迁移为 `.ts`
3. 组件保持 `.jsx`，新组件使用 `.tsx`
4. 配置 `tsconfig.json` 的 `allowJs: true` 渐进迁移

**优先迁移**：`stores/*.js` → `stores/*.ts`（状态类型最关键）

**影响**：开发效率、重构信心、IDE 补全体验

---

### 35. CI/CD 流水线

**问题**：项目没有 CI/CD 配置（无 `.github/workflows`、无 Dockerfile）。所有构建、测试、部署都是手动操作。

**方案**：

```yaml
# .github/workflows/ci.yml
name: CI
on: [push, pull_request]
jobs:
  backend:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: pip install -r requirements.txt
      - run: pytest
  
  frontend:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: npm ci
      - run: npm run lint
      - run: npm run build
```

**同时建设**：

```
项目根目录/
├── Dockerfile              # 后端容器化
├── docker-compose.yml      # 前后端 + DB 一键启动
└── .github/workflows/
    ├── ci.yml              # 持续集成
    └── deploy.yml          # 自动部署
```

**影响**：代码质量保障、部署效率、团队协作

---

### 36. 请求限流（Rate Limiting）

**问题**：所有 API 端点和 WebSocket 没有任何限流措施。LLM 调用成本高昂，恶意用户可以通过高频请求快速消耗 API 额度。

**方案**：

```python
from slowapi import Limiter

limiter = Limiter(key_func=get_remote_address)

@router.post("/api/chat/sessions")
@limiter.limit("20/minute")
async def create_session(request: Request, ...):
    ...
```

WebSocket 侧也需要限制每秒消息数。

**影响**：成本控制、防滥用、服务稳定性

---

### 37. 前端错误边界（Error Boundary）

**问题**：前端没有任何 `ErrorBoundary` 组件。一个组件的 JS 错误会导致整个页面白屏。

**方案**：

```jsx
// 全局错误边界
<ErrorBoundary fallback={<ErrorFallback />}>
  <App />
</ErrorBoundary>

// 关键组件级错误边界
<ErrorBoundary fallback={<PDFReaderError />}>
  <PDFReader />
</ErrorBoundary>

<ErrorBoundary fallback={<ChatError />}>
  <ChatPage />
</ErrorBoundary>
```

**影响**：用户体验、错误可恢复性

---

### 38. 国际化（i18n）基础设施

**问题**：所有 UI 文本硬编码为中文，无法切换语言。后续如果需要支持英文用户或学术国际交流场景，改动量巨大。

**方案**：

1. 引入 `react-i18next`
2. 提取所有硬编码文本到 `locales/zh-CN.json` 和 `locales/en-US.json`
3. 后端 API 错误消息也需支持多语言

```
frontend/src/
├── locales/
│   ├── zh-CN.json
│   └── en-US.json
├── i18n.js                  # i18next 配置
```

**影响**：用户覆盖面、学术国际化场景

---

### 39. 数据库连接池优化

**问题**：`database.py` 的引擎创建使用默认连接池参数，没有针对异步场景调优：

```python
engine = create_async_engine(
    settings.DATABASE_URL,
    echo=settings.DEBUG,
    future=True
    # 缺少 pool_size、max_overflow、pool_recycle 等关键参数
)
```

SQLite 在并发场景下容易遇到 `database is locked` 错误，后续迁移 PostgreSQL 时更需要合理配置。

**方案**：

```python
engine = create_async_engine(
    settings.DATABASE_URL,
    echo=settings.DEBUG,
    future=True,
    pool_size=20,           # 常驻连接数
    max_overflow=10,        # 峰值额外连接
    pool_recycle=3600,      # 连接回收时间（秒）
    pool_pre_ping=True,     # 连接健康检查
    connect_args={"check_same_thread": False}  # SQLite 专用
)
```

**影响**：并发稳定性、数据库连接管理

---

### 40. 优雅关停（Graceful Shutdown）

**问题**：后端没有处理 `SIGTERM`/`SIGINT` 信号，关停时可能中断正在进行的 LLM 流式输出、RAG 索引构建等长任务，导致数据不一致。

**方案**：

```python
import signal

async def graceful_shutdown(app):
    # 1. 停止接受新连接
    # 2. 等待进行中的请求完成（设置超时）
    # 3. 保存所有缓存数据
    # 4. 关闭数据库连接
    await close_db()
```

**影响**：数据一致性、服务可靠性

---

### 41. 数据备份与导出机制

**问题**：没有数据库备份策略，也没有用户数据导出功能。SQLite 文件损坏或误操作可能导致所有数据丢失。

**方案**：

1. 自动定时备份：每日备份 SQLite 文件 + ChromaDB 目录
2. 用户数据导出 API：导出论文元数据、高亮、笔记、对话记录
3. 数据导入 API：支持从备份恢复

```python
# 备份端点
@router.post("/api/settings/backup")
async def create_backup(db: AsyncSession = Depends(get_db)):
    backup_data = {
        "papers": [...],
        "highlights": [...],
        "notes": [...],
        "chat_sessions": [...],
        "chat_messages": [...],
    }
    return JSONResponse(content=backup_data)
```

**影响**：数据安全、用户信任

---

### 42. Feature Flag 系统

**问题**：新功能（Agent 模式、知识图谱、多 Agent 协作等）上线时无法灰度发布，要么全量上线要么不上线。

**方案**：

```python
# 后端：基于用户配置的功能开关
class FeatureFlags:
    @staticmethod
    async def is_enabled(feature: str, user_id: int, db: AsyncSession) -> bool:
        result = await db.execute(
            select(FeatureFlag).where(
                FeatureFlag.feature == feature,
                FeatureFlag.user_id == user_id
            )
        )
        flag = result.scalar_one_or_none()
        return flag.enabled if flag else False

# 前端：从后端获取功能开关
const { data: features } = await api.get('/api/features');
if (features.agent_mode) { /* 显示 Agent 入口 */ }
```

**影响**：灰度发布、A/B 测试、风险控制

---

### 43. 事件驱动架构基础

**问题**：当前各模块之间通过直接函数调用通信，后续添加多 Agent 协作、知识图谱自动构建等功能时，模块间通信会变得复杂。

**方案**：引入简单的事件总线，为后续功能解耦：

```python
# backend/app/events.py
class EventBus:
    _handlers = {}
    
    @classmethod
    def on(cls, event_type: str, handler):
        cls._handlers.setdefault(event_type, []).append(handler)
    
    @classmethod
    async def emit(cls, event_type: str, data: dict):
        for handler in cls._handlers.get(event_type, []):
            await handler(data)

# 使用示例
EventBus.on("paper_uploaded", rag_service.index_paper)
EventBus.on("paper_uploaded", knowledge_service.extract_entities)
EventBus.on("paper_uploaded", recommendation_service.update_recommendations)

# 论文上传后自动触发
await EventBus.emit("paper_uploaded", {"paper_id": paper.id})
```

**影响**：模块解耦、功能扩展性、多 Agent 协作基础

---

### 44. 安全加固

**问题**：当前存在多个安全隐患：

| 问题 | 位置 |
|------|------|
| JWT 密钥硬编码 | `config.py`: `JWT_SECRET_KEY = "your-secret-key-change-in-production"` |
| CORS 允许所有方法/头 | `main.py`: `allow_methods=["*"], allow_headers=["*"]` |
| 无文件上传类型校验 | 上传端点未验证文件是否真的是 PDF |
| 无 SQL 注入防护审计 | 虽然用 ORM，但部分 `text()` 语句需审查 |
| 无 HTTPS 强制 | 生产环境未配置 |

**方案**：

1. JWT 密钥从环境变量读取，启动时检查是否为默认值
2. CORS 精确配置允许的方法和头
3. 上传文件校验 magic bytes（PDF 文件头 `%PDF-`）
4. 添加安全响应头（CSP、X-Content-Type-Options 等）
5. 生产环境强制 HTTPS

**影响**：安全防护、生产环境就绪度

---

### 45. 数据库索引优化

**问题**：当前数据库索引仅在 ORM 模型中零散定义，缺少对高频查询场景的系统性索引规划。后续数据量增长后查询性能会显著下降。

**方案**：针对高频查询添加复合索引：

```python
# 论文表：用户 + 阅读状态（论文列表筛选）
Index('ix_papers_user_status', 'user_id', 'reading_status')

# 消息表：会话 + 时间（消息历史加载）
Index('ix_messages_session_created', 'session_id', 'created_at')

# 文本块表：论文 + 页码 + 位置（全文检索）
Index('ix_textblocks_paper_page', 'paper_id', 'page_number', 'y0')

# 高亮表：论文 + 页码（页面高亮加载）
Index('ix_highlights_paper_page', 'paper_id', 'page')
```

**影响**：大数据量下的查询性能

---

## 优化优先级速查表

| 序号 | 优化项 | 分类 | 优先级 | 影响维度 |
|------|--------|------|--------|----------|
| 1 | ws.py 上帝文件拆分 | 架构 | **P0** | 可维护性 |
| 2 | 数据库迁移改用 Alembic | 数据层 | **P0** | 数据安全 |
| 3 | PDF 滚动模式懒加载 | 性能 | **P0** | 内存 / 卡顿 |
| 4 | ReaderPage 并行加载 | 性能 | **P0** | 载入时间 |
| 5 | BM25 索引缓存 | 性能 | **P0** | 响应速度 |
| 6 | llm_service 提示词分离 | 架构 | **P1** | 可维护性 |
| 7 | 提取 SessionService / MessageService | 耦合 | **P1** | 代码复用 |
| 8 | WebSocket 认证 | 安全 | **P1** | 安全 |
| 9 | Markdown 组件 memo | 性能 | **P1** | 渲染性能 |
| 10 | 打字机效果优化 | 性能 | **P1** | 流畅度 |
| 11 | 跨文档检索并行化 | 性能 | **P1** | 响应速度 |
| 12 | 消息列表虚拟化 | 性能 | **P1** | 渲染性能 |
| 13 | ChatPage 组件拆分 | 架构 | **P1** | 可维护性 |
| 14 | RAG / LLM 双向依赖解耦 | 耦合 | **P2** | 架构清晰度 |
| 15 | Repository 层提取 | 架构 | **P2** | 代码复用 |
| 16 | Agent ToolContext 统一 | 架构 | **P2** | 扩展性 |
| 17 | Store 拆分 + API 层封装 | 架构 | **P2** | 前端架构 |
| 18 | RAG 索引异步重建 | 性能 | **P2** | 响应速度 |
| 19 | 全文缓存 | 性能 | **P2** | 响应速度 |
| 20 | 前端请求缓存 | 性能 | **P2** | 载入时间 |
| 21 | 会话消息分页 | 性能 | **P2** | 载入时间 |
| 22 | 重复数据库查询优化 | 性能 | **P2** | 查询效率 |
| 23 | 全局单例改依赖注入 | 架构 | **P3** | 可测试性 |
| 24 | Paper 分析缓存拆表 | 数据层 | **P3** | 数据模型 |
| 25 | 全局错误处理 / Toast | 体验 | **P3** | 用户体验 |
| 26 | 消除硬编码 user_id | 架构 | **P3** | 多用户 |
| 27 | Embedding 线程池扩容 | 性能 | **P3** | 并发性能 |
| 28 | 列表项 React.memo | 性能 | **P3** | 渲染性能 |
| 29 | WebSocket 消息压缩 | 性能 | **P3** | 带宽 |
| 30 | Paper 模型字段拆分 | 数据层 | **P3** | 数据模型 |
| 31 | 零测试覆盖 | 工程化 | **P4** | 回归保障 |
| 32 | 结构化日志系统 | 可观测性 | **P4** | 问题排查 |
| 33 | API 版本化 | 架构 | **P4** | 兼容性 |
| 34 | 前端 TypeScript 迁移 | 工程化 | **P4** | 类型安全 |
| 35 | CI/CD 流水线 | 工程化 | **P4** | 质量保障 |
| 36 | 请求限流 | 安全 | **P4** | 成本控制 |
| 37 | 前端错误边界 | 体验 | **P4** | 容错性 |
| 38 | 国际化基础设施 | 扩展性 | **P4** | 用户覆盖 |
| 39 | 数据库连接池优化 | 数据层 | **P4** | 并发稳定 |
| 40 | 优雅关停 | 可靠性 | **P4** | 数据一致 |
| 41 | 数据备份与导出 | 数据层 | **P4** | 数据安全 |
| 42 | Feature Flag 系统 | 架构 | **P4** | 灰度发布 |
| 43 | 事件驱动架构基础 | 架构 | **P4** | 模块解耦 |
| 44 | 安全加固 | 安全 | **P4** | 安全防护 |
| 45 | 数据库索引优化 | 数据层 | **P4** | 查询性能 |

---

### 建议实施阶段

```
Phase 1（1-2 周）— P0 全部完成:
├── #1  ws.py 拆分
├── #2  Alembic 迁移
├── #3  PDF 懒加载
├── #4  ReaderPage 并行加载
└── #5  BM25 索引缓存

Phase 2（2-3 周）— P1 全部完成:
├── #6   提示词分离
├── #7   SessionService / MessageService
├── #8   WebSocket 认证
├── #9   Markdown memo
├── #10  打字机效果优化
├── #11  跨文档检索并行
├── #12  消息列表虚拟化
└── #13  ChatPage 拆分

Phase 3（2-3 周）— P2 全部完成:
├── #14-17  架构解耦（RAG/LLM、Repository、ToolContext、Store）
├── #18-22  性能优化（索引异步、全文缓存、请求缓存、分页、查询优化）
└── ...

Phase 4（按需）— P3 逐步完善:
├── #23-30  依赖注入、数据模型、错误处理、并发优化等
└── ...

Phase 5（与功能开发并行）— P4 基础设施建设:
├── #31  测试框架搭建（优先：后端核心服务单元测试）
├── #32  结构化日志（替换 print，添加请求追踪）
├── #33  API 版本化（新路由加 /v1/ 前缀）
├── #34  TypeScript 迁移（优先：stores → api → 组件）
├── #35  CI/CD + Docker（自动化构建与部署）
├── #36  请求限流（保护 LLM API 额度）
├── #37  错误边界（防止白屏）
├── #38  国际化基础设施（提取文本到 locale 文件）
├── #39  数据库连接池调优
├── #40  优雅关停
├── #41  数据备份与导出
├── #42  Feature Flag 系统
├── #43  事件驱动架构基础
├── #44  安全加固
└── #45  数据库索引优化
```
