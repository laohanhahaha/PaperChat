"""服务层相关提示词（memory_service + knowledge_service）"""

# ============ 记忆服务提示词 ============

MEMORY_EXTRACTION_PROMPT = """你是一个智能记忆提取助手。请从以下对话中提取值得长期记忆的信息。

用户问题：{question}
助手回答：{answer}

请分析对话中是否包含以下类型的信息：
1. 用户的研究方向或学术兴趣（如"我对机器学习感兴趣"、"我在研究NLP"）
2. 用户的偏好（如"请用简单语言解释"、"我喜欢详细的回答"）
3. 重要的学术概念理解（如用户对某个术语的特殊理解或用法）
4. 用户的背景信息（如"我是计算机专业学生"、"我在做毕业论文"）

请以JSON格式输出提取的记忆列表（如果没有值得记忆的信息，返回空数组）：
[
  {
    "type": "research_interest|preference|term_usage|background",
    "content": "记忆内容的具体描述"
  }
]

注意：
- 只提取明确表达的信息，不要推测
- 内容要简洁具体
- 如果没有值得记忆的信息，返回 []"""

# ============ 知识库服务提示词 ============

AUTO_TAG_PROMPT = """你是一个专业的学术知识标签生成助手。请为以下学术内容生成 3-5 个相关标签。

要求：
1. 标签应简洁、准确，能概括内容的核心主题
2. 使用中文或英文专业术语
3. 返回格式：每行一个标签，不要编号
4. 不要生成过于宽泛的标签（如"学术"、"研究"）

内容：
{content}

请直接输出标签列表（每行一个）："""

FIND_RELATIONS_PROMPT = """你是一个知识关联分析助手。请分析新知识与现有知识卡片之间的关联关系。

新知识：
标题：{new_title}
摘要：{new_summary}

现有知识卡片列表：
{existing_cards}

请分析新知识与哪些现有卡片存在关联，并返回 JSON 数组格式：
[
  {{"target_card_id": 卡片ID, "relation_type": "关联类型", "description": "关联描述", "confidence": 0.9}}
]

关联类型可选：related（相关）、prerequisite（前置知识）、extends（扩展）、contradicts（矛盾）、supports（支持）

只返回 JSON 数组，不要有其他内容。"""

EXTRACT_FROM_HIGHLIGHT_PROMPT = """你是一个学术知识提取助手。请从以下高亮文本中提取关键知识点。

高亮文本：
{highlight_text}

请生成以下内容：
1. 标题（简洁概括，不超过 30 字）
2. 内容（完整知识点描述，保留原文含义）
3. 摘要（一句话概括核心要点）

返回 JSON 格式：
{{"title": "标题", "content": "内容", "summary": "摘要"}}"""

EXTRACT_FROM_CHAT_PROMPT = """你是一个学术知识提取助手。请从以下问答内容中提取关键知识点。

问答内容：
{chat_content}

请生成以下内容：
1. 标题（简洁概括，不超过 30 字）
2. 内容（完整知识点描述）
3. 摘要（一句话概括核心要点）

返回 JSON 格式：
{{"title": "标题", "content": "内容", "summary": "摘要"}}"""
